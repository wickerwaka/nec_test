db
greybox_tmp
incremental_db
output_files
# BUILD OUTPUT, deleted by `sw/quartus_gate.py`'s own clean build.  It held one
# TRACKED file, `quartus_gate.json`, so every branch switch resurrected a fossil
# receipt beside a newer build's reports -- and a receipt is an OUTPUT, never a
# checked-in fact.  The surviving history is sw/testdata/receipts/*.jsonl.
output_files_ucore
simulation
hc_output
scaler
hps_isw_handoff
vip
*_sim
.qsys_edit
PLLJ_PLLSPE_INFO.txt
*.bak
*.orig
*.rej
*.qdf
*.rpt
*.smsg
*.summary
*.done
*.jdi
*.pin
*.sof
*.qws
*.ppf
*.ddb
build_id.v
# Quartus COMPILE TRANSCRIPTS.  They are hashed into each receipt's `reports`
# manifest, so a receipt already names the bytes it read -- but they run to
# ~1.3 MB per sweep and the durable artifacts (per-seed receipts, .sta.summary,
# the truefmax tables, distribution.json) live in sw/testdata/g6dist/.
quartus_gate_build.log
g6dist_*.log
c5_pin_model_dump.txt
*.sopcinfo
*.csv
*.f
*.cmp
*.sip
*.spd
*.bsf
*~
*.xml
*_netlist
*.cdf
**/.DS_Store