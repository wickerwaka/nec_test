# SURVEY FIX #4 — `C2` RESULTS, AS REGISTERED

Pre-registration `docs/notes/fz2_c2_prereg_2026-08-10.md` (committed
`4a65d2cd68`, **before** the RTL was touched) and amendment C2-1
`docs/notes/fz2_c2_amend_2026-08-10.md` (committed `a1d55970e1`, with the RTL
**reverted** and **before** the replacement was built).

**`sim/` is not touched.**  Offline: no board, no flash.  Isolated worktree
`agent-a23530fa14330ed64`.

**⚠ EVERY NUMBER IN §6.2 ONWARD IS RE-MEASURED ON THE `C1`-MERGED TREE.**
Survey fix #3 (`C1`) landed on `fuzz-v2-on-relanding` while this sitting was
running; this work is **rebased onto `f30a8c2643`** and **both legs were
re-measured there** — see §9, which also answers the coordinator's interaction
question with a measurement.  §6.1's refuted-form figures are the only ones
taken on the pre-`C1` tree, and they are labelled as such.

---

## 1. HEADLINE

**The brief's `C2` is not one family and not the mechanism it names.**  It is
**7 + 4 + 1**, the seven are one law, and the law is not *"the re-recognition
window after an acknowledge is one boundary too wide"* — it is that
**`intr_pending`, the latch that remembers a request present when IE rises, has
no expiry.**

**The registered form of the fix — delete the retention — was built, scored and
REFUTED BY ITS OWN FALSIFIER**: silicon *does* acknowledge a request whose pin
has already gone (`fz2c/404040`, chip acknowledge at row 654 with the pin down
since 647).  It was reverted.

**What landed is ONE TERM**, `&& !ie_p[3]`, which makes the latch's own
twelve-month-old comment — *"retained … until this floor matures"* — true.
**Two seats close, one improves, four are left OPEN and NAMED, and ZERO seeds
are lost anywhere.**

---

## 2. THE LANDING

`hdl/rtl/ucore/v30u_eu.sv`, and nothing else in the RTL:

```
-wire irq_int_lvl = (int_p[2] ||
-                    (intr_pending && (rep_kind == REP_NONE))) &&
-                   ie_p[2] && psw[FIE];
+wire irq_int_lvl = (int_p[2] ||
+                    (intr_pending && (rep_kind == REP_NONE) && !ie_p[3])) &&
+                   ie_p[2] && psw[FIE];
```

**No flop added or removed.  No save-state address.  No new state.  No opcode
named.  No register introduced** — `ie_p[3]` already exists and this module
already uses it for exactly this meaning, beside `eu_bnd_post`: *"`!ie_p[3]` is
exactly 'the IE gate is what held this boundary', since the take needs
`ie_p[2]` and IE was still low four clocks back."*  A latch armed at clock `A`
is readable at `A+1 … A+3` and dead from `A+4`: the three-clock floor, and
nothing longer.

`intr_pending` keeps its IE-rise setter, its older REP-withdrawal writer
(`v30u_eu_cond.svh`), its `C_INTR` reader, its flop and its address.

---

## 3. THE INSTRUMENT — `sw/fz2_c2_rescore.py`, NEW, AND IT IS WHY THE FAMILY WAS DIAGNOSABLE AT ALL

`tb_v30_core` has ONE pin-event scheduler; every fz2 capture uses at least two.
`sw/fz2_a1_rescore.py` spends the TB's scheduler on the campaign's TERMINATING
NMI — correct for a family reached without a pin, and it means **the seed's own
stimulus pin is never asserted there, so `C2` is invisible to it.**  369 of the
725 retained captures carry a stimulus event (252 INT, 117 NMI).

`fz2_c2_rescore` spends the scheduler on the STIMULUS and cuts the comparison
window at `min(banked win, the terminating NMI's own assertion row)` — a rule
fixed before any result was seen.  **CONTROL: on all twelve `C2` seeds it
reproduces the BANKED first-divergence row and signature exactly** (244, 527,
1331, 636, 227, 611, 1475, 589, 235, 584, 1265, 472).

It is a DELTA instrument, like `fz2_a1_rescore`: its absolute `bad` counts are
smaller than the banked ones because the window is shorter and they are **not**
comparable to the corpus bars.

---

## 4. THE PARTITION — **THE BRIEF IS WRONG ABOUT THE FAMILY AND THE ARTIFACT WINS**

| sub | n | seeds | what parts |
|---|---|---|---|
| **C2a** | 7 | `fz2c/405002` `fz2c/405013` `fz2c/405072` `fz2e/511030` `fz2e/512056` `fz2e/513019` `fz2e/516038` | one extra ENTRY (two extra `INTA` cycles), carried by `intr_pending` with the pin released |
| **C2b** | 4 | `fz2c/404071` `fz2e/514044` `fz2e/516001` `fz2e/516065` | the recognition is ON TIME; the first acknowledge cycle is issued **+7 / +9 / +16 / +17** clocks late.  Three are a HALT wake (a `HALT` status is the last cycle before the divergence on all three); one is a REP withdrawal (`rep_kind = 3` at the divergence) |
| **C2c** | 1 | `fz2c/410047` | **no recognition at all** — `irq_int_lvl` never rises in 4,200 clocks.  2-clock hold, the only raw-tier and only non-`FUNCTIONAL` member, and already named in §38.6 as `C1` counter-population |

Two further errata against `fz2_corpus_prereg_2026-08-08.md` §38.6, both
confirmed on the rows: the remainder is **five** seeds not four (`fz2e/516065`
runs **2** `INTA` cycles on BOTH legs — its divergence is *when*, not *how
many*), and the count relation is chip `N` → core `N+2` because **one whole
extra ENTRY** runs, not because anything acknowledges on both edges.

**§38.6's own `C2` falsifier — *"a seed where the chip runs the extra pair"* —
is NOT fired**: on all seven the chip's `INTA` sequence is a strict prefix of
the core's.

---

## 5. THE EVIDENCE

### 5.1 The defect, on the rows

`intr_pending` is set in block (a) on the clock IE rises if the pin was up one
clock earlier, and is cleared **only by an interrupt entry**
(`v30u_eu_step.svh:838`).  Worked exemplar `fz2c/405072`: the pin falls at clock
629, IE rises at 629, the latch is set at 630, and at 632 `irq_int_lvl` is high
**solely** because of it (`int_p[2]` = the pin at 629 = 0).  The core enters at
636.  Silicon does not.

On **six of the seven** the latch-carried window opens at exactly `pin fall + 3`
— `int_p`'s own depth.  On the seventh (`fz2e/516038`) it opens **282 clocks**
after the fall, because IE was down in between: that is the demonstration that
the lifetime was unbounded and not merely three clocks too long.

### 5.2 The census — **NINE LOAD-BEARING CLOCKS IN 252 CAPTURES**

Baseline ucore, every clock at which `irq_int_lvl` is high while `int_p[2]` is
low, over all 252 INT-stimulus captures.  `arm` is the clock `intr_pending`
rose:

| seed | hold | silicon | `arm` | latch-carried run | **`run` − `arm`** | entry | **after this landing** |
|---|---|---|---|---|---|---|---|
| `fz2c/404040` | 300 | **AGREES** | 648 | 650–651 | **2** | 654 | **PRESERVED** |
| `fz2e/531000` | 2 | **AGREES** | 1060 | 1063 | **3** | 1067 | **PRESERVED** |
| `fz2c/405002` | 300 | disagrees | 521 | 523–524 | **2** | 528 | OPEN |
| `fz2c/405013` | 300 | disagrees | 1327 | 1329–1330 | **2** | 1334 | OPEN |
| `fz2c/405072` | 300 | disagrees | 630 | 632–633 | **2** | 637 | OPEN |
| `fz2e/512056` | 300 | disagrees | 1467 | 1469–1470 | **2** | 1476 | OPEN |
| `fz2e/513019` | 300 | disagrees | 565 | 570–585 | **5** | 590 | **IMPROVED** |
| `fz2e/511030` | 300 | disagrees | 361 | 576–612 | **215** | 615 | **CLOSED** |
| `fz2e/516038` | 300 | disagrees | 920 | 1209–1257 | **289** | 1266 | **CLOSED** |

**`run` − `arm` is the only column that separates anything.**  Checked and NOT
separating: the arm's offset from the pin fall (`+1` on both sides), the
window's opening offset (`pin fall + 3` on eight of nine), the window length,
`rep_kind` (0 throughout), the hold, and the take's offset inside the window
(`+1` throughout).  `fz2c/404040` and the four `run − arm == 2` seats are
**identical on every coordinate in the recognition path** and silicon does
opposite things on them.

> **REPORTED AS A FINDING, NOT AS A DEFEAT.  On the evidence available offline
> there is no predicate at the recognition decision that separates
> `fz2c/404040` from those four.**  Closing them needs a directed board cell on
> the IE-rise / pin-fall race, which does not exist; deriving one from these
> nine rows would be fitting.

### 5.3 Silicon acknowledges after its own pin falls — engine-free

Chip rows only, all **252** INT-stimulus captures: **31 of the 251 chip
acknowledge runs begin at or after the INT pin's own fall**, at `+0 … +40`
clocks.  27 are the `hold = 2` population, where a 2-clock pulse makes it
unavoidable; **four are `hold = 300`**, in three seeds — `fz2c/405056` (+0),
`fz2c/404040` (+7), `fz2c/405040` (+18 and +40).  *This is what refuted the
registered form, and it is why the retention is kept.*

---

## 6. THE BARS, AS REGISTERED

### 6.1 The REFUTED registered form (prereg §4.5) — reported because it was run

| bar | registered | measured |
|---|---|---|
| P-1 all 7 seats strictly improve | yes | **MET** (6 to `bad == 0`, 1 improved) |
| P-2 ≥ 5 reach `bad == 0` | ≥ 5 | **MET — 6** |
| P-3 the five non-`C2a` UNMOVED | cell for cell | **MET** |
| P-4 **ZERO LOST over 725** | 0 | **MISSED — 1 lost**, `fz2c/404040`, a banked `SUCCESS`, `bad` 0 → 1,325 at row 651 |

Corpus CLEAN 574 → **579**.  `r7_lint` PASS, `ss_lint` PASS/UNMOVED on that
build (receipt `e6830c489bc6a33d…`).  **No further gate was run on it — a
refuted form is not promoted to the ladder — and the RTL was reverted before
amendment C2-1 was written.**

### 6.2 The LANDED form (amendment §C2-1.4) — **EVERY BAR MET**

**Measured on the `C1`-MERGED tree** (`f30a8c2643` + this landing).  BEFORE leg
binary receipt `f225b7393bb75af2…` / `e6e6cd6b936757aa…`, AFTER leg
`8d3ba99e64d2b020…` / `9f51c54471315ed3…` (same sources rebuild to the same
output sha256; the receipt id is the build event).  **The pre-`C1` tree gave
the identical delta, seed for seed and value for value** — §9.

| bar | registered | **measured** | |
|---|---|---|---|
| **Q-1** | `fz2e/511030` and `fz2e/516038` reach `bad == 0` | **`bad` 649 → 0** and **431 → 0**, `first` → `None` | **MET** |
| **Q-2** | `fz2e/513019` `bad` 2,545 → 2,484, `first` 589 → 656 | **2,545 → 2,484**, **589 → 656**, `sig` `'bs CODE!=INTA;nxta'` → `'qs -!=F'` | **MET, to the value** |
| **Q-3** | `405002` `405013` `405072` `512056` UNCHANGED cell for cell | `bad` 840/921/433/984 and `first` 527/1331/636/1475 — **identical** | **MET** |
| **Q-4** | `fz2c/404040` PRESERVED at `bad == 0` | **`bad == 0`** | **MET** |
| **Q-5** | ZERO LOST / 0 earlier / CLEAN 574 → 576 | **LOST 0, EARLIER 0, CLEAN 574 → 576**; **only 3 seeds moved in all 725 and all three are named seats** | **MET** |
| **Q-6** | `fz2_a1_rescore` ZERO LOST, CLEAN ≥ 352 | **CLEAN 352 → 352, MOVED 0 of 725** | **MET** |
| **Q-7** | the ladder unchanged | §6.3 — every cell identical to the merged BEFORE | **MET** |
| **Q-8** | G6 PASS on two draws, 39–40 MHz, ALMs within ±0.5 % of control | §6.4 | **MET** |

Amendment falsifier #2 (*"if any of those four closes, the attribution is
wrong"*) is **NOT fired**; falsifier #3 (*"if a seed outside the nine census
rows moves at all"*) is **NOT fired** — the moved set is exactly
{`511030`, `513019`, `516038`}.

### 6.3 The ladder — **BOTH LEGS ON THE `C1`-MERGED TREE**

| gate | merged BEFORE | **merged AFTER** | |
|---|---|---|---|
| `check_core --opcodes all --cases 0` | 169,000 / 169,000 | **169,000 / 169,000**, 0 non-perfect forms | MET |
| `v0.1-w0evt --waits 0` | 200 / 200 | **200 / 200** | unchanged |
| `v0.1-w1evt --waits 1` | 1,169 / 1,200 | **1,169 / 1,200** | unchanged |
| `v0.1-w2evt --waits 2` | 177 / 200 | **177 / 200** | unchanged |
| `v0.1-w3evt --waits 3` | 1,176 / 1,200 | **1,176 / 1,200** | unchanged |
| `v0.1-w1evt-biased --waits 1` | 1,163 / 1,200 | **1,163 / 1,200** | unchanged |
| HLT sweeps w0/w1/w2/w3 | 97 / 93 / 45 / 44 = 279 / 283 | **97 / 93 / 45 / 44 = 279 / 283** | MET |
| `r7_lint` | PASS | **PASS** — 0 carriers, 0 unresolved, 0 `stop` sites, no new exception | MET |
| `ss_lint` / `ss_flopcensus` | PASS, 226 addresses / **214** flops | **PASS, 226 / 214, UNMOVED** | MET |
| `test_artifact` | 45 / 45 | **45 / 45** | MET |
| `gen_ucore_qsf --check` | up to date | **up to date** | MET |
| `check_boot --timed 220` / `--timed 400` | MATCH / MATCH | **MATCH / MATCH** | MET |
| `ulockstep --golden all --cases 50` | — | **17,350 / 17,350, ALL CASES LOCKSTEP** | INFORMATIONAL (the model is defunct by user ruling) |
| `fz2_a1_rescore` (terminator, 725) | CLEAN 352 / 725 | **CLEAN 352 / 725, errors 0 — MOVED 0** | **Q-6 MET** |
| `fz2_c1_rescore` (the `C1` sitting's own instrument, 725) | CLEAN **339** / 725, SCOREABLE 396 | **CLEAN 339 / 725, SCOREABLE 396 — MOVED 0** | **`C1`'s family is untouched by this landing** |

Corpus dirty count on the stimulus instrument: **151 → 149**.

⚠ **Read the terminator leg for what it is.**  It is the ZERO-CHANGE control:
the seven seats cannot move there, because that instrument spends the TB's only
scheduler on the terminating NMI and never asserts the stimulus pin.  Its value
here is exactly that **nothing else moved either** — 0 of 725 rows differ in
`bad`, `first` or `sig`.

### 6.4 G6 — **PASS**

`python3 sw/quartus_gate.py`, configuration **CONTROL / DEFAULT** (no
`X1_AD_RETENTION`), derived by the gate from the flow and map reports, `db`
deleted first (not an incremental compile).

**ON THE `C1`-MERGED TREE — these are the draws that gate this landing:**

| draw | Fmax | worst setup | TNS setup **and** hold | errors | latches | `lpm_divide` | ALMs | receipt |
|---|---|---|---|---|---|---|---|---|
| **1** | **39.16 MHz** | **+5.714 ns** | **0.000** (E5 empty) | 0 | 0 | 0 | **12,339 / 41,910 (29 %)** | `98eae323b642f29c…` |
| **2** | **39.16 MHz** | **+5.714 ns** | **0.000** (E5 empty) | 0 | 0 | 0 | **12,339 / 41,910 (29 %)** | `ff35603bcc0b7149…` |

**The two draws are IDENTICAL** in Fmax, worst setup, TNS, ALMs and the 88-file
input manifest (`b1e091aa7437e8f7…` on both); only the reports' own sha differs
(`a3c7efc46cc85677…` / `789070c347ee8ca6…`, they carry timestamps).  E1
`gen_ucore_qsf --check` **PASS** on both.

*On the PRE-`C1` tree the same landing drew **40.12 MHz / +6.322 ns / TNS 0.000
/ 12,166 ALMs**, receipt `6ec4fa037dc31b5b…`, manifest `49a328c30a139676…` —
recorded because it was run, superseded as a gate by the merged draws above.*

**Q-8 in context.**  The branch's own CONTROL band is 39.37 MHz / 12,340 ALMs
(`CLAUDE.md`); the `C1` sitting's own draws are **39.63 MHz / 12,330 ALMs**,
identical on both of its draws.  This landing draws **39.16 MHz / 12,339
ALMs** — inside the registered 39–40 band, **+9 ALMs (+0.07 %) against `C1`'s**,
inside the registered ±0.5 %.  ⚠ **One green build is not closure**, and −0.47
MHz against `C1`'s draw is **not** attributable to one added AND term:
`standing_gates.md` §A / `ucore_provenance.md` §74.4a record that the same tree
has drawn 19.42 and 45.91 MHz and that A&S combinational counts are not
reproducible run to run.

---

## 7. ⚠ A BASELINE FINDING THAT IS NEITHER THIS LANDING'S NOR A1's

The brief names the four `evt` golden cells and `w1evt-biased` as the
maskable-recognition populations that must hold at **200 / 1,200 / 200 / 1,200**
and **1,200** (`standing_gates.md` §B).  **On the tree as inherited they are
RED, before any edit of mine:**

| cell | standing | measured baseline | arch |
|---|---|---|---|
| `v0.1-w0evt` | 200 | **200 / 200** | 200 |
| `v0.1-w1evt` | 1,200 | **1,169 / 1,200** | 1,200 |
| `v0.1-w2evt` | 200 | **177 / 200** | 200 |
| `v0.1-w3evt` | 1,200 | **1,176 / 1,200** | 1,200 |
| `v0.1-w1evt-biased` | 1,200 | **1,163 / 1,200** | 1,200 |

Every failure is `INT.F3AA` with the signature **`row 28 qop: exp 'E' got
'-'`** (23 of 200 at w2); **`arch` is PERFECT on all four cells**, so it is a
queue-status display divergence, not a functional one.

**IT PREDATES THE A1 LANDING.**  Controlled by checking out A1's only RTL file
(`hdl/rtl/ucore/v30u_biu.sv`) at `483c1f1270^`, rebuilding and re-running:
**1,169 / 177 / 1,176 / 1,163 — identical, cell for cell.**  It is not A1's and
it is not this sitting's.  It is reported because `CLAUDE.md`'s branch banner
lists what was re-measured green on this branch and **these four cells are not
in that list**, so the standing figures are a pre-branch measurement that no
gate on this branch has confirmed.  A coordinator decision is needed on whether
to re-register them at the measured value or open it as a work item.

---

## 8. WHAT IS OPEN AFTER THIS SITTING

1. **Four `C2a` seats** — `fz2c/405002`, `fz2c/405013`, `fz2c/405072`,
   `fz2e/512056`.  All four are `run − arm == 2`, i.e. inside the floor this
   landing keeps open, and all four are indistinguishable from `fz2c/404040`
   (where silicon *does* acknowledge) on every coordinate measured.  **The next
   step is a directed board cell on the IE-rise / pin-fall race, not more
   offline scoring.**
2. **`C2b`, 4 seeds** — the acknowledge is issued 7 to 17 clocks late after a
   HALT wake (3) or a REP withdrawal (1).  Untouched, and adjacent to A1's own
   registered remainder.
3. **`C2c`, 1 seed** — `fz2c/410047`, already booked as `C1` counter-population.
4. **The `evt` cells** (§7) — a pre-existing branch red with no owner.

*Nothing here is claimed as explained.  Every open item has its population
named so a later sitting can pre-register against it.*

---

## 9. THE `C1` MERGE — **THE TWO LANDINGS ARE INDEPENDENT, AND THAT IS MEASURED, NOT ASSUMED**

Survey fix #3 (`C1`, `97ca50861c`…`f30a8c2643`) landed on
`fuzz-v2-on-relanding` while this sitting was running.  This work is **rebased
onto `f30a8c2643`**; the rebase's only conflicts were the three append-only
receipt ledgers, resolved by union (no line dropped, all rows re-validated as
JSON).

### 9.1 The two edits do not overlap

Both are in `hdl/rtl/ucore/v30u_eu.sv` and they are **970 lines apart**: `C1`'s
hunk is at line 1782 (`brk_seen` / the five `S_IRQ_D` entry sites, plus
`v30u_eu_step.svh` and `v30u_eu_row.svh`), this one at line 809
(`irq_int_lvl`).  This landing touches **no** file `C1` touched except that one,
and **no** signal it touched — `brk_arm`, `brk_p`, `brk_smp`, `irq_sel_brk` and
`SSA_E_BRK` are untouched here, `intr_pending` and `ie_p` are untouched there.

### 9.2 The delta is IDENTICAL on both trees

`fz2_c2_rescore`, 725 captures, BEFORE and AFTER re-measured on the merged tree:

| | pre-`C1` tree | **`C1`-merged tree** |
|---|---|---|
| CLEAN before → after | 574 → 576 | **574 → 576** |
| seeds MOVED | 3 | **3 — the same three** |
| `fz2e/511030` | 649 → 0 | **649 → 0** |
| `fz2e/513019` | 2,545 → 2,484, first 589 → 656 | **2,545 → 2,484, first 589 → 656** |
| `fz2e/516038` | 431 → 0 | **431 → 0** |
| LOST / EARLIER | 0 / 0 | **0 / 0** |

**`C1`'s own effect on THIS instrument is one seed** — `fz2e/531036`, a `C1`
family member, `bad` 1,805 → 2 with its first divergence unmoved — and it is
disjoint from all three of this landing's.

### 9.3 Neither landing disturbs the other's instrument

| instrument | its family | merged BEFORE | merged AFTER | moved |
|---|---|---|---|---|
| `sw/fz2_c1_rescore.py` (`C1`'s own) | terminator-armed, IVT-2 patched | CLEAN 339 / 725, SCOREABLE 396 | **CLEAN 339 / 725, SCOREABLE 396** | **0 of 725** |
| `sw/fz2_a1_rescore.py` | terminator-armed | CLEAN 352 / 725 | **CLEAN 352 / 725** | **0 of 725** |
| `sw/fz2_c2_rescore.py` (this one) | stimulus-armed | CLEAN 574 / 725 | **CLEAN 576 / 725** | 3, all named seats |

**The three instruments are complementary by construction and each is blind to
the others' family**: `C1`'s and `A1`'s spend the TB's single scheduler on the
terminating NMI (`fz2_c1_rescore`'s own docstring states *"a seed with a
STIMULUS pin event never gets it"*), and this one spends it on the stimulus and
stops before the terminator exists.  A future sitting should keep all three.

### 9.4 The interaction question, answered

The coordinator asked whether the acknowledge-consumption law is *"spent /
not-spent state of the same class as the BRK arm — two laws with one shape may
be one law."*

**They are the same CLASS and they are NOT the same law, and the corpus says so
by arithmetic**: the two deltas are disjoint seed sets, they are additive, and
each is unchanged by the other's presence (§9.2, §9.3).  Stated precisely:

* `C1` is a **CONSUMER** question — *which event spends `brk_arm`*.  Its answer
  is that an external recognition does not, only the trap's own take does; the
  arm's lifetime was already correct.
* `C2` is a **LIFETIME** question — *how long `intr_pending` may substitute for
  the pin*.  Its answer is three clocks, the floor it was written for; its
  consumer (the interrupt entry) was already correct.

They meet at the same place — the bookkeeping of a remembered event at the
interrupt entry — and a later sitting may find one statement covering both.
**This sitting does not claim one**, because the two mechanisms have no seed in
common and neither changes the other's numbers by a single row, which is the
evidence a unification would have to overturn rather than the evidence for one.
