# FLASH #15 — THE WAVE-2 MEASUREMENT SITTING FOR `P1` + `P2`
# PRE-REGISTRATION, committed BEFORE any board contact

Branch `fuzz-v2-on-relanding`, HEAD **`dc2f86b625`**.  The board carries
**FLASH #14** (`060215e43c5de9b3…`, flashed 2026-08-10T14:27:59Z from
`2fa3dd33b3-dirty`), which **predates both of this wave's RTL landings**.
Under the standing rule **no fabric figure taken on FLASH #14 may be quoted
against this tree**, and nothing below is a fabric claim until FLASH #15 is
verified on the board.

The two landings, in the order they reached the branch:

| # | landing | commit | RTL touched |
|---|---|---|---|
| 1 | **P2** — `L-A` a boundary that walks through the external door still samples the arm; `L-B` `HLT` does not park when it retires into the trap | `a275b9553c` | `v30u_eu.sv` (×2), `v30u_eu_step.svh` (×1) |
| 2 | **P1** — one arbiter, one rail: the REP withdrawal's arbitration point reads the recognition, not the maskable pin | `19d2fc2b82` | `v30u_biu.sv` (one term, shrinks by one) |

**Verified before writing this**: `git diff 2fa3dd33b3 dc2f86b625 -- hdl/rtl/`
touches exactly **three files** — `v30u_biu.sv`, `v30u_eu.sv`,
`v30u_eu_step.svh` — and every hunk in it is one of the two landings above
plus its comment block.  **The bitstream this sitting builds is FLASH #14's
RTL plus P1 plus P2 and nothing else.**

---

## 0. FOUR CORRECTIONS TO THE BRIEF, STATED BEFORE ANYTHING IS MEASURED

The brief is accurate on its headline and it is incomplete in four places.
The artifact wins.

**0.1 — THE WAVE REPLAY ARTIFACT IS NOT IN THE REPOSITORY.**  The brief names
`scratchpad/wave_replay.json` as a source for the seat lists.  That file lives
in the session scratchpad
(`/tmp/claude-1000/…/45f40932-…/scratchpad/wave_replay.json`) and **a reviewer
cannot open it.**  Every list it supplies is therefore **transcribed in full
below** and cross-checked seed for seed against the two results documents
(`fz2_p1_results_2026-08-10.md` §2, `fz2_p2_results_2026-08-10.md` §5A.1),
which are committed.  The transcription was verified by set equality: P1's 10
∪ P2's 19 **equals** the artifact's `fabric_fail_replay_pass` list exactly, 29
of 29, with no seed in both.

**0.2 — THE SAME APPLIES TO THE `M9a`/`M9b` COUNTER-POPULATION.**  P1's
pre-registration cites `f14_partition_final.json` for its 29 counter-population
seeds; that file is also scratchpad-only.  **All 29 are transcribed in §4.6.**
All 29 are FLASH #14 ledger failures and **none of them is one of this wave's
29 seats** (intersection empty, checked).

**0.3 — SEVENTEEN OF THE 29 SEATS CARRY A DIFFERING ARCHITECTURAL DUMP, AND
THE REPLAY SCORES ROWS ONLY.**  `fz2_replay` returns bus rows; a seed leaves
the ledger only if its rows match **and** its dump matches.  Of the 29, **12
carry `arch = OK`** — for those a rows-close *is* a ledger exit — and **17
carry a differing or absent dump**.  That split is this sitting's principal
registered uncertainty and it is registered as such in §5.2's FLOOR, **not
discovered afterwards**.  (F14 had the same structure at 14 of 46 and all 14
converged; that is a precedent, not a proof.)

**0.4 — THREE OF THE REGISTERED MOVERS MOVE THEIR FIRST DIVERGENCE *EARLIER*,
AND THAT IS NORMALLY A REFUTATION CONDITION.**  `fz2c/406073`, `fz2e/518044`
and `fz2c/407064` are each predicted to keep their ledger seat with
`first_bad_row` **decreasing** (−1, −1, −9).  Both landings' results documents
registered this in advance (`p2_results` §2.2 and §5A.3) with the mechanism
named.  It is registered here **as an expected, itemized exception**, and any
first-divergence decrease **outside these three seeds** is a refutation.

---

## 1. THE BUILD — G6, RETENTION CONFIGURATION (`E-*`)

Manual flow, because `quartus_gate.py`'s own `build()` runs
`quartus_sh --flow compile` with no `--verilog_macro` and structurally cannot
produce a retention build.  Per draw, from deleted `db` / `incremental_db` /
`output_files_ucore`:

    quartus_map --verilog_macro=X1_AD_RETENTION=1 nec_test -c nec_test_ucore
    quartus_fit  nec_test -c nec_test_ucore
    quartus_asm  nec_test -c nec_test_ucore
    quartus_sta  nec_test -c nec_test_ucore
    python3 sw/quartus_gate.py --parse-only --no-qsf-check --label "fz2 FLASH#15 RETENTION ret_drawN"

| # | bar | registered |
|---|---|---|
| **E-1** | `gen_ucore_qsf.py --check` **BEFORE** each draw | PASS (measured PASS at HEAD at registration) |
| **E-2** | 0 errors, every stage `Successful`, 0 latches, 0 `lpm_divide` | both draws |
| **E-3** | `divclk` Fmax | **≥ 32 MHz**, both draws |
| **E-4** | worst setup slack | **> 0 ns**, both draws |
| **E-5** | TNS setup **AND** hold | **0.000 on every domain**, both draws |
| **E-6** | the receipt **self-labels RETENTION** | `configuration` begins `RETENTION (X1_AD_RETENTION=1) -- DERIVED from …flow.rpt, …map.rpt`, `configuration_detail.retention == true` |
| **E-7** | the two draws agree | identical Fmax / worst setup / TNS / ALMs and an identical 88-file input manifest; the `.rbf` sha256 is the bitstream identity and is expected identical (the `.sof` is stamped and may differ) |
| **E-8** | `gen_ucore_qsf.py` regenerated and re-checked **AFTER** each draw | PASS |

**E-6 is a HARD STOP if it reads `CONTROL/DEFAULT`** — the `4bb65d2ab6` defect;
the bitstream may not be flashed on it.

### 1.1 THE RETENTION-VS-CONTROL SIGN ANOMALY — REGISTERED AS AN OBSERVATION, NOT A BAR

**This tree's CONTROL is 40.00 MHz / +6.249 ns, two identical draws**
(`dc2f86b625`, receipts `2026-08-10T17:19:32Z` and `T17:29:06Z`).
Historically retention drew at or below control (SM3 s19 45.49 → 44.99; s27
47.85 → 45.72; W4 47.31 → 46.74; this branch's own base 41.80 → 40.96).  **On
this branch it has now inverted twice**: FLASH #13 control 39.37 → retention
39.83, and FLASH #14 control 39.47 → retention 40.97.

**I register NO Fmax point estimate and no band.**  `standing_gates.md` §A
governs — the same tree has drawn 19.42 and 45.91 MHz, the multi-seed
worst-of-N gate is still not built, and **one green build is not closure and
two are not either**.  E-3/E-4/E-5 are the bars.  **What is registered is the
reporting obligation**: the sitting will state the sign of
(retention − control) on this tree explicitly, and it will be the third
observation in the series regardless of which way it falls.

---

## 2. THE FLASH AND FIRST LIGHT (`F-*`)

| # | bar | registered |
|---|---|---|
| **F-1** | `sw/safe_flash.sh <ucore .sof>` — PREP / `quartus_pgm` / VERIFY | VERIFY OK; `sw/testdata/flash_log.jsonl` **17 → 18 entries**, tail `verify: "OK"` |
| **F-2** | first light `check_ab_hw.py all 800` | **MATCH over 800 rows on all three legs** (chip-vs-golden, core-vs-chip, core-vs-golden), `rc 0` |
| **F-3** | RBCHECK | **exactly 8 registers** round-trip: `EVT_ADDR[0..2]`, `EVT_CFG[0..2]`, `TVEC`, `VECCTL` |
| **F-4** | C-6 control, `fz2_w1.py control` | **9 legs / 51 checks / 51 PASS**, `holds_proved [2, 20, 300]`, `pins_proved [pin_int, pin_nmi]`, `tvecs_proved [[0,48896],[3056,8]]`, P1–P5 run lengths **2 · 300 · 2 · 300 · 20** to the clock, INTA vector `0xFF`, N1 negative control PASS |
| **F-5** | `use_core=0` chip proof **after everything** | `check_ab_hw.py chip 800` → **MATCH over 800 rows** |
| **F-6** | `div_guard()` tally | **0 UNPINNED** — that is the bar.  Expected count ≈ **63** = preflight 2 + control 10 + preflight 2 + capture 48 + idle 1 (F14's measured decomposition); the count itself is reported, not barred |
| **F-7** | transport | **0 `RigMismatch`**, 0 transport errors |
| **F-8** | `board_idle()` | clean, `use_core=0` left selected |

**F-2 justification, registered in advance.**  P1 needs a REP string
interrupted by a recognition at a staged withdrawal; P2 needs `PSW.TF` standing
at a retire boundary that dispatches an interrupt, or a `HLT` retiring into an
armed trap.  The boot program contains **none of the three** — no REP, no
`PSW.TF`, no acknowledge — so **neither landing is reachable by it** and MATCH
800 is the correct prediction.  Any deviation is a hard stop.

**F-4 IS SCORED ON ITS VERDICTS, NOT ON ROW SHA256** (f13 §10.4: two `control`
runs on the *same* flash give 0 of 8 identical row sha256s and 51/51 PASS both
times).  Row-sha movement is **not** a stop.

---

## 3. THE CORPUS RE-CAPTURE (`G-*`)

Archive by rename first — the SUP discipline, **nothing deleted**:

    sw/testdata/campaigns/fz2c          -> fz2c-F14-archive
    sw/testdata/campaigns/fz2e          -> fz2e-F14-archive
    sw/testdata/fz2/fz2_capture.json    -> fz2_capture_F14-archive.json
    sw/testdata/fz2/fz2_bars.json       -> fz2_bars_F14-archive.json
    sw/testdata/fz2/fz2_preflight.json  -> fz2_preflight_F14-archive.json
    sw/testdata/fz2/fz2_control.json    -> fz2_control_F14-archive.json

then `fuzz_campaign.py new fz2c` / `new fz2e` (re-pins each manifest to the
resident flash), `fz2_w1.py preflight --board`, `fz2_w1.py capture`.

| # | bar | registered |
|---|---|---|
| **G-1** | corpus identity unchanged | `SEED_LIST_SHA256 = 45d25f31a325c496…`, 48 strata, `fz2c` 960 + `fz2e` 2,880 = **3,840** |
| **G-2** | preflight | `verdict: "OK"`, `board_leg: true`, 0 GEN_DRIFT on the regeneration sample |
| **G-3** | capture completeness | **48 / 48 strata**, `rc 0`, no `halted`, `results.jsonl` **960 / 2,880** rows |
| **G-4** | retained captures | **NOT BARRED — reported.**  F14's `725` bar was mis-registered as a constant and missed at 677; retention on the enriched tier is **divergence-driven** (`keep_rows_every = 0`), so **a seed that stops diverging stops being retained**.  20 of this wave's 29 seats are `fz2e`, so the count is **predicted to FALL** and a fall is the sitting working.  The bar is only that **every seed named in any prediction below is still retained and scored** |
| **G-5** | the flash pin | every result row's `era.sof_sha256` = FLASH #15's `.sof` sha256; `distinct_eras 1`, `absent 0`, `build_stale 0` over 3,840 |
| **G-6** | discards | **1**, `fz2e/509069`, denominator **3,839** — **and this is a prediction, not an assumption** |

**G-6's basis and the re-roll process.**  `_ps3_8080()` is a **SOCKET-leg**
predicate (amendment A-2): it reads the chip column, not the core's, so a core
landing cannot move it by construction.  FLASH #14 measured it True on exactly
**1 of 3,840**.  **But it moved at F14** — `fz2c/408029` and `fz2e/523042`
flipped out of the discard set, and §4 of the F14 results attributed that to
those seeds being ESCAPED programs executing open bus, which have no
reproducible bus.  `fz2e/509069` is in the same escaped population.  **The
registered process for a move is A-12 / A-13's and it is invoked here in
advance**: a new or vanished discard is **itemized loudly, seed by seed, with
its `escaped_n`**, the denominator moves with it, **and the rate is reported on
BOTH bases** — the F14 denominator of 3,839 for the ratchet and the moved one
as derived.  **A discard re-roll is NOT a hard stop and NOT a ratchet
violation.**

---

## 4. THE SEAT-LEVEL PREDICTIONS (`P-*`) — THE HEART OF THIS SITTING

**The instrument is `sw/fz2_replay.py`** — the faithful `tb_sys` replay: three
pin-event schedulers, `TVEC`, `VECCTL` and the seed's own wait source, i.e. the
same directives the rig hands the board.  Its BEFORE leg on the base tree
(`fz2_replay_fidelity_2026-08-10.json`, era guard ON, receipt
`33204aaa1364…`) reproduces the fabric verdict on **295 of 295** and the fabric
`first_bad_row` on **145 of 145 failures**.  That control is what makes this a
seat-exact prediction rather than a directional one.

The AFTER leg on the combined tree was run at `13b22302d0-dirty`, `--leg ret`,
`--all-failures`, `--no-fabric-era-guard` (deliberately, on both legs — the
board carries neither tree), tb_sys receipt `d039f58ab9f830af…`:

    replay PASS 29   replay FAIL 116   LOST 0   seeds with MORE bad rows 1
    first_bad identical on 111 of the 116 that still fail

### 4.1 — **P-1: THE 29 SEATS CLOSE, SEED FOR SEED**

Ledger coordinates are FLASH #14's (`fz2_failure_ledger_f14_2026-08-10.json`).
`arch` is quoted so §5.2's FLOOR can be read off it.

**P1's ten** (all ledger family `A1 qs-pop one clock late`, **all `arch = OK`**):

| seat | `first_bad_row` | `diverging_rows` | `arch` |
|---|---|---|---|
| `fz2c/408000` | 3253 | 350 | OK |
| `fz2c/409066` | 3322 | 278 | OK |
| `fz2c/410073` | 3039 | 373 | OK |
| `fz2e/518024` | 3318 | 302 | OK |
| `fz2e/518065` | 3318 | 302 | OK |
| `fz2e/518068` | 3330 | 278 | OK |
| `fz2e/519027` | 3264 | 429 | OK |
| `fz2e/523046` | 3020 | 366 | OK |
| `fz2e/526025` | 3166 | 373 | OK |
| `fz2e/528076` | 3273 | 429 | OK |

**P2's nineteen:**

| seat | ledger family | `first_bad_row` | `diverging_rows` | `arch` |
|---|---|---|---|---|
| `fz2c/407024` | C1 | 3058 | 539 | AW |
| `fz2c/409062` | C1 | 3346 | 387 | AW,BP,CW,DW,IX,IY |
| `fz2c/410062` | C1 | 3096 | 617 | AW,BW,IX |
| `fz2e/507064` | C1 | 1904 | 729 | IX |
| `fz2e/518046` | C1 | 3346 | 361 | AW |
| `fz2e/518072` | C1 | 3358 | 391 | BW |
| `fz2e/519032` | C1 | 3298 | 483 | BW |
| `fz2e/530030` | C1 | 3158 | 736 | BP,CW |
| `fz2e/531000` | C1 | 1138 | 2607 | OK |
| `fz2c/407057` | B2 | 967 | 2609 | AW,BW,CW,DS1,DW,IX,PC,PS,PSW,SP |
| `fz2c/410000` | B2 | 2525 | 1139 | AW,BW,CW,DW,PC,PSW |
| `fz2c/410053` | B2 | 1207 | 2775 | NODUMP |
| `fz2e/521036` | B2 | 2980 | 778 | AW,BW,CW,DW,IY,PC,PS,PSW,SP |
| `fz2e/522071` | B2 | 3006 | 694 | BP,BW,PC,PS,PSW,SP |
| `fz2e/529036` | B2 | 2928 | 833 | AW,BW,DW,IY,PC,SP |
| `fz2e/529055` | B2 | 2142 | 1759 | OK |
| `fz2e/535042` | B2 | 819 | 2895 | PC,PS,PSW,SP |
| `fz2e/517043` | NEW/UNCLASSIFIED | 2773 | 619 | NODUMP |
| `fz2e/531009` | NEW/UNCLASSIFIED | 3325 | 577 | AW,BP,BW,DW,IY |

**P-1 REGISTERED: all 29 leave the ledger.**  The 29 carry **25,008** of the
ledger's 150,998 diverging rows.

**P-1a, a corollary worth its own line:** `fz2e/517043` and `fz2e/531009` are
**two of the three seeds that ENTERED the ledger at FLASH #14** (F14 §5.5, a
registered MISS).  `fz2e/517043` is the `PSW.TF` seed booked to `C1`'s
undischarged directed-cell debt.  **Both are predicted to close, on a landing
that names no `M3` term** — P2 §5A.2's finding that the two "over-fires" were
the same missing sample seen from the other side.  **If they do not close, that
finding is refuted and `M3` is re-opened.**

### 4.2 — **P-2: THE FIVE REGISTERED MOVERS THAT MUST *NOT* CLOSE**

Every one keeps its ledger seat; the movement is the prediction.

| seed | family | `first_bad_row` F14 → predicted | `diverging_rows` F14 → predicted | source |
|---|---|---|---|---|
| `fz2c/406073` | B2 | 1575 → **1574** (−1) | 2116 → **≈5** | p2 §2.2 / §5A.3 |
| `fz2e/518044` | B2 | 2609 → **2608** (−1) | 1117 → **≈5** | p2 §2.2 / §5A.3 |
| `fz2c/407064` | **B1** | 1956 → **1947** (−9) | 1680 → **≈998** | p2 §5A.3 (the brief's named case) |
| `fz2c/407065` | A1 | 3046 → **3053** (+7) | 401 → **≈422** | p1 §2.2 / §3 |
| `fz2e/523045` | A1 | 3033 → **3040** (+7) | 375 → **≈396** | p1 §2.2 / §3 |

The first three are the **only** permitted first-divergence *decreases* in the
corpus (§0.4).  `406073` and `518044` are one prefetch cycle — the trap cancels
the HALT display and the park but **not** the prefetch suspend, blocked on a
`v30u_biu.sv` port-list split that P2 deliberately did not take.  `407065` and
`523045` are **EU post timing**, P1 §2.2's registered residual with its own
falsifier.

**P-2b — one seed gains a row and it is not a regression**: `fz2e/518067`
(family `E1`, mechanism `M10`), `first_bad_row` **325, UNMOVED**,
`diverging_rows` 3,277 → **3,278**.  Downstream noise in a replay forked since
row 325.  Registered because P1's own bar said "increased 0" and one increased.

### 4.3 — **P-3: THE PREDICTED FAMILY TABLE**

| ledger family | F14 | predicted F15 | exits |
|---|---|---|---|
| A1 qs-pop one clock late | 15 | **5** | 10 |
| A2 qs-pop other offset | 4 | 4 | 0 |
| A3 cycle-time slip (non-qs) | 15 | 15 | 0 |
| B1 HALT-cycle address | 1 | 1 | 0 |
| B2 HALT entry (one leg only) | 10 | **2** | 8 |
| C1 vector-1 trap MISSED by core | 10 | **1** | 9 |
| C2 INTA-vectored delivery | 10 | 10 | 0 |
| C3 NMI(vec2) entry | 1 | 1 | 0 |
| C4 other-vector delivery | 1 | 1 | 0 |
| D1 chip fetched, core did not | 10 | 10 | 0 |
| D2 core fetched, chip did not | 10 | 10 | 0 |
| D3 both fetched, different address | 10 | 10 | 0 |
| E1 same-status data cycle, different address | 41 | 41 | 0 |
| E2 different-status data cycle | 4 | 4 | 0 |
| NEW/UNCLASSIFIED | 3 | **1** | 2 |
| **total** | **145** | **116** | **29** |

The named survivors, so a reviewer can check the table rather than trust it:
`C1` keeps **`fz2e/518039`** alone; `B2` keeps `fz2c/406073` and `fz2e/518044`;
`A1` keeps `fz2c/407065`, `fz2e/509036`, `fz2e/513055`, `fz2e/518009`,
`fz2e/523045`; `NEW/UNCLASSIFIED` keeps `fz2e/532032`.

### 4.4 — **P-4: THE §38.9 MISSED-TRAP OVERLAY FALLS 21 → 4**

The overlay was 40 at FLASH #13 and 21 at FLASH #14.  **Predicted 4**:
`fz2c/406073`, `fz2c/407064`, `fz2e/518039`, `fz2e/518044`.  It stops being the
largest single mechanism in the corpus; `E1` (41) becomes it.

### 4.5 — **P-5: `fz2c/404040` MUST NOT APPEAR IN THE NEW LEDGER**

The sharpest falsifier this branch has.  It is a banked SUCCESS on FLASH #14
and it is the seed whose silicon behaviour **refuted the `C2` sitting's
registered form**.  Neither P1 nor P2 touches its mechanism.  P2 §2.3 measured
it unmoved on all four offline legs (`first 351 / bad 1620` on that instrument,
identical before and after).  **If it is a failure on FLASH #15, a landing in
this wave has broken a mechanism nobody claimed, and that is a landing-level
finding.**

### 4.6 — **P-6: THE COUNTER-POPULATIONS — 36 SEEDS, ZERO MOVEMENT**

All 36 are FLASH #14 ledger failures.  **Registered: none of them exits the
ledger, and `first_bad_row` and `diverging_rows` are UNCHANGED on all 36.**
A move here is the fitted-in-the-wrong-place signal these populations exist to
catch.

**`M9a` + `M9b`, bus-slot arbitration (29 seeds)** — P1's own registered
counter-population, disjoint from every seat above:

    fz2c/404049  fz2c/405025  fz2c/407000  fz2c/407067  fz2c/408068
    fz2c/409077  fz2e/501069  fz2e/510043  fz2e/510048  fz2e/511014
    fz2e/512062  fz2e/513026  fz2e/515047  fz2e/516026  fz2e/516066
    fz2e/520005  fz2e/520013  fz2e/527008  fz2e/527017  fz2e/527065
    fz2e/530017  fz2e/530020  fz2e/531030  fz2e/532000  fz2e/532021
    fz2e/532066  fz2e/534062  fz2e/535004  fz2e/535027

**The §64.1 counter-population (4 seeds**, one of which — `fz2e/535027` — is
also in the `M9` list, so the union is 36 not 37):

| seed | family | `first_bad_row` | `diverging_rows` |
|---|---|---|---|
| `fz2c/406063` | E1 | 249 | 3168 |
| `fz2c/410047` | C2 | 227 | 3589 |
| `fz2e/518053` | E1 | 571 | 3409 |
| `fz2e/535027` | E1 | 296 | 3226 |

**The four `C2a` seats**, registered OPEN by the `C2` landing and inside the
floor it deliberately keeps open:

| seed | family | `first_bad_row` | `diverging_rows` |
|---|---|---|---|
| `fz2c/405002` | C2 | 527 | 840 |
| `fz2c/405013` | C2 | 1331 | 921 |
| `fz2c/405072` | C2 | 636 | 891 |
| `fz2e/512056` | C2 | 1475 | 984 |

**Row counts inside these 36 MAY move by downstream noise and a small move is
reported, not a violation** — but a **ledger EXIT** from any of the 36 is a
finding and is itemized seed by seed.

### 4.7 — **P-7: ZERO LOSSES.  NO CURRENTLY-MATCHED SEED MAY BECOME A FAILURE**

The replay measured **LOST 0** over all 145 ledger failures, and its BEFORE leg
agreed with fabric on **145 of 145 failures plus 150 sampled passes**.  But the
AFTER leg was run on the 145 failures ONLY: **the 3,694 currently-matched seeds
have not been replayed on this tree.**  That is this sitting's registered
exposure, exactly as it was F14's — and **F14 missed the same bar with three
seeds**.  A new failure is a **finding of the first order**, itemized seed by
seed with its family, `first_bad_row` and whether its chip leg moved.

### 4.8 — **P-8: NO UNREGISTERED CLOSURE**

F14 saw **ten** unregistered closures because its offline instruments were
structurally blind (one pin-event scheduler, no `TVEC`).  `fz2_replay` is not:
it has all three schedulers and it reproduced fabric 295/295.  **Registered:
exits from the ledger are EXACTLY the 29 named in §4.1 — no more and no
fewer.**  An unregistered closure is therefore **an instrument finding about
`fz2_replay`**, not a bonus, and it will be reported as one.

---

## 5. THE REGISTERED HEADLINE

### 5.1 THE PRIMARY

On the FLASH #14 denominator of **3,839** (1 discard):

    SEED MATCH   3,694 + 29 = 3,723 / 3,839  =  96.9784 %
    failures     145 -> 116

Like for like on the **3,837** basis the ratchet has been quoted on:

    SEED MATCH   3,692 + 29 = 3,721 / 3,837  =  96.9769 %

### 5.2 THE REGISTERED FLOOR

If the 17 arch-carrying seats close their rows but not their dumps (§0.3):

    SEED MATCH   3,694 + 12 = 3,706 / 3,839  =  96.5356 %
    failures     145 -> 133

**A result at or above 3,706 with the 12 `arch = OK` seats all closed is the
two landings performing as measured.  A result below 3,706, or ANY seed leaving
the 3,694, is a MISS and will be reported as registered.**

If the rows close and the arch does not, **that is a finding about the offline
instrument's blindness to the dump, not a failure of the landings**, and it will
be reported against this floor rather than restated.

### 5.3 THE ROW FIGURE — DIRECTIONAL, NOT TO THE DIGIT

    F14           rows_diverging 150,998 / 11,330,230  =  98.6673 % match

The 29 seats delete **25,008** rows; §4.2's movers subtract a further ~3,905
(`406073` −2,111, `518044` −1,112, `407064` −682) and add ~44 (`407065` +22,
`523045` +21, `518067` +1).  **Point estimate ≈ 122,100 diverging rows,
≈ 98.92 % row match**, a rise of ≈ 0.25 pp.

**§38.4's rule is invoked explicitly**: SEED MATCH and ROW MATCH must both be
reported, and a landing set that raises one while lowering the other must say
so.  Only the seed figure is a ratchet.

### 5.4 HOW THE LEDGER IS RE-DERIVED

`python3 sw/fz2_ledger.py` with its **acceptance control run first**:
`--control --suffix -F13-archive` must reproduce the committed A-15 ledger
(198 failures as a set, 14 family counts, `first_bad_row` 198/198,
`diverging_rows` 198/198, 3 discards, denominator 3,837).  **If the control does
not reproduce, the new ledger is not quotable and the finding is the tool, not
the corpus.**  `--chip-control -F14-archive` is run as C-CHIP.

**C-CHIP is registered exactly as A-1 registered it**: a failure is a hard stop
for **attribution**, not for the sitting, and a partial failure must be itemized
seed by seed.  F14's C-CHIP missed as registered and read **673/674 on the rows
the comparator scores**, with every mover an escaped seed; the same reading is
expected and the same disposition applies.

### 5.5 THE CLOSING CONTROL — THE FIDELITY CHECK **WITHOUT** THE ERA OVERRIDE

`python3 sw/fz2_replay.py --all-failures --pass-sample 150 --leg ret` against
the **newly derived** ledger, **era guard ON**.  On FLASH #15 the board carries
this tree's RTL, so the guard must pass by arithmetic rather than by flag.

**Registered: the replay agrees with fabric on every seed it scores, and
`first_bad_row` matches on every seed both legs fail.**  This is the statement
that the offline instrument this whole wave's attribution rests on is faithful
to the silicon it now shares an era with.  **A disagreement here is an
instrument finding and it is reported before any seat claim is quoted.**

---

## 6. WHAT THIS SITTING DOES NOT CLAIM

* **Nothing about `sim/`.**  Not touched; `ulockstep` informational at most.
* **Nothing about `wr1`, `b2`, the four `_v1_anchor_stop` timed gates.**  All
  `GEN_DRIFT` or anchor-broken on this branch.
* **No mechanism claim for the 116 survivors.**  `E1` (41), the `D*` families
  (30) and `A3` (15) are untouched and unexplained, exactly as they were.
* **No Fmax closure.**  §1.1.
* **No claim that the arch dump is predicted.**  §0.3 / §5.2.
* **Nothing about the P1 residual or the P2 prefetch remainder.**  Both are
  booked with falsifiers in their own results documents and neither is opened
  here.

---

## 7. HARD STOPS — halt and report, as nine predecessors did

1. `safe_flash` **VERIFY failure** → **physical power cycle**, no blind retry,
   no bridge access.
2. Any `div_guard` **UNPINNED** readback.
3. Any **`RigMismatch`**.
4. **5 consecutive quarantines** (`fuzz_campaign.cmd_run`'s circuit breaker,
   left armed) or any **short stratum** (`written < 80`).  Not nursed.
5. Any capture-integrity bar failing (G-1 … G-5).
6. First light below **MATCH 800** on any of the three legs.
7. **E-6**: a retention build whose receipt reads `CONTROL/DEFAULT`.
8. **Any contradiction inside this pre-registration.**

**Not stops**, each reported with its denominator and carried: C-6 row-sha
movement (§2); a discard re-roll (§3, G-6); a retained-capture count that falls
(§3, G-4); a first-divergence decrease on the three seeds named in §0.4 / §4.2;
a ROW MATCH that falls while SEED MATCH rises (§5.3); C-CHIP failing partially
with its movers itemized (§5.4).

---

## 8. TREE STATE AT REGISTRATION

* HEAD **`dc2f86b625`**, branch `fuzz-v2-on-relanding`, **`hdl/rtl/` clean**.
* `sw/testdata/receipts/verilator_binary.jsonl` carries uncommitted
  append-only rows from the P1/P2 sittings' builds.  Recorded, not tidied.
* `gen_ucore_qsf.py --check`: **PASS** at registration.
* **Single writer**: `git worktree list` shows six stale agent worktrees and
  one detached tree, with **no running process in any of them**; no `quartus`,
  `verilator` or `v30ctl` process was running when this was written.
* `sw/testdata/flash_log.jsonl`: **17 entries**, tail = FLASH #14
  (`060215e43c5de9b3…`).
* `sw/testdata/fz2/fz2_failure_ledger_f14_2026-08-10.json` — **to be left
  byte-identical**; the re-derivation writes a NEW file.
* CONTROL G6 on this tree: **40.00 MHz / +6.249 ns / TNS 0.000 setup AND hold
  on all four domains / 6,527 fit registers / 0 latches / 0 `lpm_divide`**, two
  identical draws (§1.1).  **The ALM count is deliberately not quoted**: the
  receipt schema records `fit_registers` and not ALMs, the fit report those
  draws produced was deleted by the retention draw's clean `db`, and
  `standing_gates` §A's rule — a number with no artifact id is not quotable —
  applies to my own convenience as much as to anyone else's.
