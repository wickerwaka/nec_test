# FLASH #15 — RESULTS, AS REGISTERED

Pre-registration `docs/notes/fz2_f15_prereg_2026-08-10.md` (**`a89951a00c`**,
committed before the retention draws returned a number and before any board
contact), addendum A-1 (**`77838ef777`**, the G6 receipts and the merged tree's
gate set, committed before the flash), and the scorer
`sw/fz2_f15_score.py` (**`af2a00b3f9`**, **written and committed while the
capture was still running and before its ledger existed**).

Every bar is reported in the form it was registered in.  **Nothing is
re-registered after the fact.**

---

## 0. THE HEADLINE, IN ONE PARAGRAPH

**Every registered clause is MET except one, and the one that missed is a
denominator.**  All **29 registered seats closed, seed for seed** — including
**all 17 that carried a differing architectural dump**, which §0.3 registered as
the sitting's principal uncertainty.  **Zero seeds entered the ledger**
(F14 missed this with three).  **Zero unregistered closures** (F14 had ten).
The predicted family table is **exact in all fifteen cells with every named
survivor correct**; the §38.9 missed-trap overlay fell **21 → 4** and the four
are the four that were named; the five registered movers hit their predicted
`first_bad_row` **exactly, to the row, on all five**; the 36-seed frozen
counter-population did not move by a row; and `fz2c/404040`, the falsifier, is
still absent.  The corpus goes **3,694 → 3,722 of a denominator that moved from
3,839 to 3,838**, and the single seed between the measurement and the registered
primary of 3,723 is **`fz2e/506039`, which this sitting measured NON-REPRODUCING
five times out of five on this very bitstream** (§5).  The closing control is
the strongest thing here: **`fz2_replay` now agrees with fabric 266 / 266 with
the era guard ON and no override, and `first_bad_row` is identical on 116 of
116.**

---

## 1. THE BUILD — G6, RETENTION, TWO DRAWS (`E-1` … `E-8`)

Reported in full in addendum A-1 (`77838ef777`) and summarised here.

| draw | verdict | Fmax (`divclk`) | worst setup | TNS setup/hold | ALMs | receipt |
|---|---|---|---|---|---|---|
| 1 | **PASS** | **42.24 MHz** | **+7.574 ns** | **0.000 / 0.000, every domain** | 12,194 / 41,910 (29 %) | `7bb5d317cf3ff6a2…` |
| 2 | **PASS** | **42.24 MHz** | **+7.574 ns** | **0.000 / 0.000, every domain** | 12,194 / 41,910 (29 %) | `c0ed51936233ee24…` |

**E-1 … E-8 ALL MET**, E-7 in its strongest form: identical 88-file input
manifest `dd51681ba98f3c67…` and a **BYTE-IDENTICAL `.rbf`**,
`82ee1f1476b6ee9a…` on both draws.  Draw 2's `.sof` `2dc38a17d6c3e77d…` is what
was flashed, and all four of its receipt-declared outputs were re-hashed on disk
first.

**§1.1's registered reporting obligation is discharged and the sign went the
same way a THIRD time**: CONTROL **40.00** → RETENTION **42.24** (+2.24 MHz),
after FLASH #13's 39.37 → 39.83 and FLASH #14's 39.47 → 40.97.  Six compiles,
two per side per landing, against a prior history in which retention drew at or
below control every time.  **Reported, not explained**, with the falsifier
written in A-1.1a.  **No Fmax claim and no band; `standing_gates.md` §A governs.**

---

## 2. THE FLASH AND FIRST LIGHT (`F-1` … `F-8`)

**FLASH #15 is on the board.**  `nec_test_ucore.sof`
**`2dc38a17d6c3e77de9f69983dbdca41bee682d25db3128ba924548822283f3e6`**, `.rbf`
**`82ee1f1476b6ee9a…`**, built from **`77838ef777`** WITH
`X1_AD_RETENTION=1` (draw 2), through `sw/safe_flash.sh` with its VERIFY leg.
**IT IS THE FIRST BITSTREAM TO CARRY `P1` AND `P2`.**

| # | bar | registered | measured | verdict |
|---|---|---|---|---|
| F-1 | flash + ledger | VERIFY OK, `flash_log.jsonl` 17 → 18 | **VERIFY ok on try 1** (`pwr_good True`, `cpu_running True`, `use_core False`), **18 entries** | **MET** |
| F-2 | first light | MATCH 800 on all three legs | **chip-vs-golden MATCH 800 · core-vs-chip MATCH 800 · core-vs-golden MATCH 800**, `rc 0` | **MET** |
| F-3 | RBCHECK | exactly 8 registers | **8** — `EVT_ADDR[0..2]`, `EVT_CFG[0..2]`, `TVEC`, `VECCTL` | **MET** |
| F-4 | C-6 control | 9 legs / 51 checks / 51 PASS | **51 / 51**, `failing_legs []` | **MET** |
| F-5 | `use_core=0` chip proof after everything | MATCH 800 | **MATCH over 800 rows** | **MET** |
| F-6 | `div_guard` tally | 0 UNPINNED | **63 guards, 0 UNPINNED** | **MET** |
| F-7 | transport | 0 `RigMismatch` | **0**, and 0 run-error lines (`C-10`) | **MET** |
| F-8 | `board_idle()` | clean, `use_core=0` left | **OK, `use_core = 0` left selected** | **MET** |

**F-4 in detail**, scored on its verdicts as registered:

    R1  RBCHECK                                  1/1
    P1  pin_int  hold 2      run [[547, 2]]      5/5
    P2  pin_int  hold 300    run [[547, 300]]    5/5
    P3  pin_nmi  hold 2      run [[547, 2]]      5/5
    P4  pin_nmi  hold 300    run [[547, 300]]    5/5   <- the INV-1 axis
    P5  pin_nmi  hold 20     run [[547, 20]]     5/5
    V1  TVEC 0000:bf00 -> 0bf00                  9/9
    V2  TVEC 0bf0:0008 -> 0bf08                  9/9
    N1  vecsub_en = 0 negative control           7/7
                                        total   51/51

`holds_proved [2, 20, 300]` · `pins_proved [pin_int, pin_nmi]` ·
`tvecs_proved [[0, 48896], [3056, 8]]` · P1–P5 run lengths **2 · 300 · 2 · 300 ·
20 to the clock**, every one starting at row 547 · 10 div guards, 0 unpinned.

**F-2's registered justification held**: neither landing is reachable by the
boot program (no REP interrupted at a staged withdrawal, no `PSW.TF`, no
acknowledge), so MATCH 800 was the correct prediction and it is what happened.

**The div-guard tally, itemised** (F-6): control 10 + preflight 2 + capture 48 +
the `fz2e/506039` repeat-capture probe 2 + idle 1 = **63**, **UNPINNED 0**.
`board_idle` was run **last**, after the chip proof, as at FLASH #14.

---

## 3. THE RE-CAPTURE (`G-1` … `G-6`)

The FLASH #14 corpus was archived **by rename** first — nothing deleted:
`fz2c` → `fz2c-F14-archive`, `fz2e` → `fz2e-F14-archive`, and
`fz2_capture` / `fz2_bars` / `fz2_control` / `fz2_preflight` `.json` →
`*_F14-archive.json`.

| # | bar | registered | measured | verdict |
|---|---|---|---|---|
| G-1 | corpus identity | `SEED_LIST_SHA256 45d25f31a325c496…`, 3,840 | **identical**, 960 + 2,880, 48 strata | **MET** |
| G-2 | preflight | `verdict OK`, `board_leg true`, 0 GEN_DRIFT | **OK**, board leg run, **192 seeds hits = 0**, era pin verified against receipt `c0ed5193…` | **MET** |
| G-3 | completeness | 48/48 strata, `rc 0`, no `halted`, 960 / 2,880 rows | **48/48**, `halted: null`, **3,840 seeds in 11.1 min**, no short stratum, `rc 0` everywhere | **MET** |
| G-4 | retained captures | **not barred** (§3) — reported | **656** (`fz2c` 496 + `fz2e` 160), down from 677 | **as registered** |
| G-5 | flash pin | every row's era = FLASH #15 | **`distinct_eras 1`, `absent 0`, `incomplete 0`, `build_stale 0`** over 3,840 (bar C-4) | **MET** |
| G-6 | discards | **1**, `fz2e/509069`, denominator 3,839 | **2** — `fz2e/509069` **and `fz2e/506039`**; denominator **3,838** | **MISSED — §5** |

**G-4 was deliberately not barred this time and the reason held.**  F14
registered 725 as a constant and missed at 677; the enriched tier's retention is
divergence-driven, so a seed that stops diverging stops being retained.  656 is
the arithmetic consequence of 29 more seeds closing, and **every seed named in
any prediction was still retained and scored.**

---

## 4. THE TWO ACCEPTANCE CONTROLS

### 4.1 `C-LEDGER` — **PASS, 9 of 9**

`python3 sw/fz2_ledger.py --control --suffix=-F13-archive`, the re-derivation
run against the FLASH #13 archive and required to reproduce the committed A-15
ledger:

    PASS  failure SET identical      ref 198  got 198  symdiff []
    PASS  denominator 3,837          PASS  matched 3,639
    PASS  discards 3                 PASS  14 family counts identical
    PASS  first_bad_row 198/198      PASS  diverging_rows 198/198
    PASS  arch 196/198               (the two known OK/NODUMP residuals, named)
    PASS  overlay 38.9 = 40

**The derivation is quotable.**

### 4.2 `C-CHIP` — **FAILS AS REGISTERED, AND THE CORRECTED READING IS 654 / 655**

`python3 sw/fz2_ledger.py --chip-control=-F14-archive`:

    chip field arch_words       3,839 / 3,840      fz2e/506039
    chip field ps3_8080         3,839 / 3,840      fz2e/506039
    chip fields done_real / wrote_term / wrote_term_at / arch_ok / arch_restart
                                3,840 / 3,840      ALL PASS
    retained chip ROW streams     268 / 655        FAIL as registered

**AS REGISTERED, C-CHIP FAILS.  It is reported as a MISS**, and A-1's rule
applies: a C-CHIP failure is a hard stop for **attribution**, not for the
sitting, and a partial failure must be itemised seed by seed.

**AND THE ROW CLAUSE READS THE SAME WAY IT DID AT FLASH #14, MEASURED AGAIN
RATHER THAN ASSUMED.**  Of the **387** retained chip streams that differ,
**386 differ ONLY at row indices ≤ 8** — the comparator's own pipeline prefix
(`check_boot`'s mission-G policy takes `qs` from release, `bs` from release+8
and `addr`/`data`/`ps` from release+9, so rows 0–8 are outside what anything
scores).  **Exactly ONE differs at a scored row: `fz2e/527051`** — the same seed
that was FLASH #14's one genuine chip mover, family `D3`, a failure in both
eras.  **On the rows the comparator scores, the chip leg reproduces on 654 of
655.**

**The two scalar movers are the same seed, `fz2e/506039`, and it is §5.**  No
seat prediction rests on either `fz2e/527051` or `fz2e/506039`; attribution
stands for the other 3,838.

---

## 5. **THE ONE MISS, AND IT IS ONE SEED: `fz2e/506039`**

**G-6 is MISSED: two discards, not one, and the denominator moved 3,839 →
3,838.**  Reported as registered, and the registered A-12 / A-13 process is
followed: itemised loudly, both bases given, no rate restated.

`fz2e/506039` — `enr/soup/noevt/wrand7`, `n_ins` 28, **`escaped_n` 0**, no
stimulus event:

| | FLASH #14 | FLASH #15 |
|---|---|---|
| verdict | **SUCCESS / clean** | **FUNCTIONAL / `func:R@13`** |
| `bad_rows` | 0 | 708 (`first_bad` 707) |
| `ps3_8080` (SOCKET leg) | False | **True** → A-12 discard |
| terminator `fired` / `vec_used` | **4 / True** | **0 / False** |
| `pin_nmi` row | **2630** | **2625** |
| chip `bus_cycles` | 227 | 232 |
| **CHIP dump** (`arch_words`) | AW 36671, PC 11337, SP 59904 … | AW 34305, PC 19580, SP 2608 … |
| **CORE dump** (`arch_sim_words`) | AW 36671, PC 11337, SP 59904 … | **BYTE-IDENTICAL to F14** |
| image sha256 · wait source · directives | — | **all identical** |

**THE CORE IS PROVABLY NOT INVOLVED.**  Its architectural dump at FLASH #15 is
byte-identical, register for register, to what BOTH legs produced at FLASH #14.
What moved is the SOCKETED CHIP: its terminating NMI landed five rows early, it
never took the terminator (`fired 0`), it ran on into a different trajectory and
entered 8080 mode, which is what set `ps3_8080` and took it out of the
denominator.

### 5.1 **AND IT IS A ONE-OFF CAPTURE, MEASURED, NOT ARGUED**

Re-captured **five times on the resident FLASH #15 bitstream**, through the
capture driver's own stratum override, with `div_guard` on both sides
(both PINNED):

    repeat 0..4   verdict SUCCESS   sub clean   bad 0   ps3_8080 False
                  fired 4   vec_used True   pin_nmi [[2630, 20]]   bus 227
                  chip AW/PC/SP 36671 / 11337 / 59904
                  core AW/PC/SP 36671 / 11337 / 59904

**Five of five reproduce the FLASH #14 answer exactly, to the clock, on the
bitstream that produced the anomalous one.**  So this is **not** era-dependent,
**not** a landing regression, and **not** the escaped-seed class FLASH #14's §4
used: it is a single non-reproducing socket capture in 3,840.

### 5.2 WHAT THAT DOES AND DOES NOT LICENSE

* **The banked figure is the figure.**  The corpus is quoted **as captured** —
  3,722 / 3,838 — and the probe is **NOT** substituted for the banked row.
  Choosing a number after seeing it is the one thing this whole discipline
  exists to prevent.
* **It is stated plainly what the difference is worth**: with that one seed at
  the value it reproduces five times out of five, the corpus would read
  **3,723 / 3,839 = 96.9784 %** — the registered PRIMARY, hit exactly.  That is
  a diagnosis of the miss, not a replacement for it.
* **P-7 (zero losses) reads MET from the ledger, and one seed's exclusion does
  the work.**  `fz2e/506039` was a matched seed at FLASH #14 and is not a
  matched seed now; A-12 takes it out of the denominator rather than into the
  ledger.  **That is stated here rather than allowed to hide inside a MET**, and
  it is why §5.1's probe was run at all.
* **REGISTERED AS AN OPEN POPULATION WITH A FALSIFIER, FOR A LATER SITTING**:
  a non-reproducing socket capture has never been characterised in this corpus.
  *Falsifier*: capture the whole 3,840 twice on one bitstream and count the
  seeds whose socket leg does not reproduce.  If that number is 0, this one
  capture is unexplained and needs a mechanism; if it is small and non-zero, the
  corpus has a measurable capture-noise floor and **every single-capture
  seed-level claim in this campaign inherits it.**  Nobody has ever measured it.

---

## 6. THE SEAT-LEVEL SCORING — `sw/fz2_f15_score.py`

`python3 sw/fz2_f15_score.py --new sw/testdata/fz2/fz2_failure_ledger_f15_2026-08-10.json`

### 6.1 **P-1: 29 OF 29 SEATS CLOSED, SEED FOR SEED**

| landing | registered | measured | verdict |
|---|---|---|---|
| **P1** `19d2fc2b82` (ledger family `A1`) | 10 | **10 / 10** | **MET** |
| **P2** `a275b9553c` (ledger family `C1`) | 9 | **9 / 9** | **MET** |
| **P2** `a275b9553c` (ledger family `B2`) | 8 | **8 / 8** | **MET** |
| **P2** `a275b9553c` (`NEW/UNCLASSIFIED`) | 2 | **2 / 2** | **MET** |
| | **29** | **29 / 29** | **MET** |

### 6.2 **THE FLOOR SPLIT — ALL SEVENTEEN ARCH-CARRYING SEATS CLOSED TOO**

§0.3 registered this as the sitting's principal uncertainty: the replay scores
rows, and 17 of the 29 carried a differing or absent architectural dump.

    arch = OK       closed 12 / 12    (a rows-close IS a ledger exit)
    arch-carrying   closed 17 / 17    (the rows-only instrument could not predict these)

**The mechanism claim is what carried it**: a dump difference that is a
*consequence* of a mis-taken or missed trap converges when the trap is taken at
the right boundary.  That is now the second sitting in a row where it held
(F14: 14 of 14).  It is a **precedent, still not a law**, and it has no
falsifier written for it yet beyond the next re-capture.

### 6.3 **P-1a: BOTH `M3` SEATS CLOSED, WITH NO `M3` TERM IN THE TREE**

`fz2e/517043` and `fz2e/531009` were **two of the three seeds that ENTERED the
ledger at FLASH #14** — a registered MISS there — and `fz2e/517043` was the one
booked to `C1`'s undischarged directed-cell debt.  Both closed.

**`P2` §5A.2's finding stands in silicon: `M3` was never a separate mechanism.**
The brief asked for three terms and the tree needed two, because two of the
three "mechanisms" were one — a core that never armed at the right boundary
arms at a later one instead and fires where the chip does not.  **The proposed
EU-vs-pin spending rule is not needed and should not be written.**

### 6.4 **P-2: THE FIVE REGISTERED MOVERS — `first_bad_row` EXACT ON ALL FIVE**

| seed | family | `first_bad_row` predicted → measured | `diverging_rows` predicted → measured |
|---|---|---|---|
| `fz2c/406073` | B2 | **1574 → 1574** (−1) | ≈5 → **5** |
| `fz2e/518044` | B2 | **2608 → 2608** (−1) | ≈5 → **5** |
| `fz2c/407064` | B1 | **1947 → 1947** (−9) | ≈998 → **1000** |
| `fz2c/407065` | A1 | **3053 → 3053** (+7) | ≈422 → **424** |
| `fz2e/523045` | A1 | **3040 → 3040** (+7) | ≈396 → **397** |

**All five kept their seats, all five hit the predicted row exactly, and the row
counts are within two of the point estimate on every one.**  The three
first-divergence *decreases* are the three §0.4 registered in advance and
**there were zero unregistered decreases anywhere in the corpus** — the
refutation condition did not fire.

**P-2b** held to the row: `fz2e/518067` `first_bad_row` **325 → 325 UNMOVED**,
`diverging_rows` 3,277 → **3,278**, exactly the +1 of downstream noise in a
replay forked since row 325.

### 6.5 **P-3: THE PREDICTED FAMILY TABLE IS EXACT IN ALL FIFTEEN CELLS**

| ledger family | F14 | predicted | **F15** |
|---|---|---|---|
| A1 qs-pop one clock late | 15 | 5 | **5** |
| A2 qs-pop other offset | 4 | 4 | **4** |
| A3 cycle-time slip (non-qs) | 15 | 15 | **15** |
| B1 HALT-cycle address | 1 | 1 | **1** |
| B2 HALT entry (one leg only) | 10 | 2 | **2** |
| C1 vector-1 trap MISSED by core | 10 | 1 | **1** |
| C2 INTA-vectored delivery | 10 | 10 | **10** |
| C3 NMI(vec2) entry | 1 | 1 | **1** |
| C4 other-vector delivery | 1 | 1 | **1** |
| D1 chip fetched, core did not | 10 | 10 | **10** |
| D2 core fetched, chip did not | 10 | 10 | **10** |
| D3 both fetched, different address | 10 | 10 | **10** |
| E1 same-status data cycle, different address | 41 | 41 | **41** |
| E2 different-status data cycle | 4 | 4 | **4** |
| NEW/UNCLASSIFIED | 3 | 1 | **1** |
| **total** | **145** | **116** | **116** |

**And the named survivors are right seed for seed** — the check that makes the
table a prediction rather than a total: `C1` keeps `fz2e/518039` alone; `B2`
keeps `fz2c/406073` and `fz2e/518044`; `A1` keeps `fz2c/407065`, `fz2e/509036`,
`fz2e/513055`, `fz2e/518009`, `fz2e/523045`; `NEW/UNCLASSIFIED` keeps
`fz2e/532032`.

### 6.6 P-4 — THE §38.9 MISSED-TRAP OVERLAY, **21 → 4, AND THE FOUR ARE THE FOUR**

Predicted `fz2c/406073` · `fz2c/407064` · `fz2e/518039` · `fz2e/518044`;
measured exactly those.  It was **40 at FLASH #13** and 21 at FLASH #14.
**It is no longer the largest mechanism in the corpus**: `E1` is, at 41 of 116
(35.3 %).

### 6.7 P-5 — **THE FALSIFIER DID NOT FIRE**

`fz2c/404040` — the seed whose silicon behaviour refuted the `C2` sitting's
registered form — is **ABSENT from the new ledger**.  Still a pass.

### 6.8 P-6 — **THE COUNTER-POPULATIONS DID NOT MOVE BY ONE ROW**

All **36** (the 29 `M9a`/`M9b`, the four §64.1, the four `C2a`, union 36):
**0 ledger exits, 0 seeds with a changed `first_bad_row` or `diverging_rows`.**
The fitted-in-the-wrong-place signal these populations exist to catch did not
appear.

### 6.9 P-7 — **ZERO LOSSES: 0 SEEDS ENTERED THE LEDGER**

Against a registered zero, measured zero.  **FLASH #14 missed this bar with
three seeds; this sitting meets it.**  ⚠ Read it with §5.2: one seed left the
matched set by becoming a discard rather than a failure, and that is stated
there rather than folded in here.

*(`fz2_ledger` prints `1 NEW FAILURE(S)` for `fz2e/532032`.  That flag is
carried relative to the **committed A-15** ledger, where the seed was a
success; it was already a failure at FLASH #14 and it is `NEW/UNCLASSIFIED`'s
predicted survivor.  **`ENTERED the ledger: 0` is the FLASH #14 comparison and
it is the one P-7 is scored on.**)*

### 6.10 P-8 — **NO UNREGISTERED CLOSURE: EXITS ARE EXACTLY THE 29**

    exits 29   registered 29   unregistered 0

**FLASH #14 had ten unregistered closures because its offline instruments were
structurally blind** (one pin-event scheduler, no `TVEC`).  `fz2_replay` is not,
and the difference shows: the prediction was seat-exact and the result is
seat-exact.  This is the clearest single measurement of what the faithful
instrument bought.

---

## 7. THE HEADLINE

### 7.1 AS DERIVED, on the FLASH #15 denominator of 3,838

    SEED MATCH   3,722 / 3,838  =  96.9776 %       failures 145 -> 116
    ROW  MATCH   11,206,546 / 11,328,714  =  98.9216 %
                 diverging rows 150,998 -> 122,168

### 7.2 AGAINST THE REGISTERED FIGURES

    registered PRIMARY  3,723 / 3,839 = 96.9784 %    measured 3,722   -1
    registered FLOOR    3,706                        measured 3,722   MET, +16
    registered failures 116                          measured 116     EXACT
    row point estimate  ~122,100 diverging rows      measured 122,168
    row match estimate  ~98.92 %                     measured 98.9216 %

**The −1 against the primary is `fz2e/506039` and nothing else** (§5).  The
FLOOR is met with 16 to spare, because the 17 arch-carrying seats closed.

§38.4's dual-reporting rule: **both figures rose.**  SEED MATCH +2.75 pp and
ROW MATCH +0.25 pp; neither was raised at the other's expense.

### 7.3 LIKE FOR LIKE ON THE 3,837 BASIS the ratchet has also been quoted on

    F14  3,692 / 3,837 = 96.2210 %   ->   F15  3,721 / 3,837 = 96.9769 %

### 7.4 THE DECOMPOSITIONS, NEVER ONE AGGREGATE

    census    rows-exact  924 ->  933 / 960     arch-exact  855 ->  863
    enriched  rows-exact 2770 -> 2789 / 2880    arch-exact 2592 -> 2603
    tier soup rows-exact 1888 -> 1889 / 1920    arch-exact 1884 -> 1885
    tier raw  rows-exact 1806 -> 1833 / 1920    arch-exact 1563 -> 1581

The **census** figures are population rates (its bank rule is frozen
arithmetic); the enriched figures are not and are never pooled with them.
**The raw tier is where this wave landed**: +27 rows-exact and +18 arch-exact
of the corpus's +28, which is the `A1`/`B2`/`C1` seats' own tier composition
(28 of the 29 seats are `raw`).

---

## 8. `bars`, LEAF-DIFFED

`python3 sw/fz2_w1.py bars --jobs 4` → **FZ2 BARS: 11 / 11 MET**, and
`python3 sw/fz2_leafdiff.py fz2_bars_F14-archive.json fz2_bars.json` confirms
leaf by leaf that **no bar verdict moved**: **1,049 leaves compared, 1 A-only,
14 B-only, 101 MOVED**, and every moved leaf is a closure gain, the new discard,
the retention count, or an archive-rename row.  The load-bearing ones:

    C-4  distinct_eras 1 / absent 0 / build_stale 0 over 3,840   (G-5)
    C-8  div_guards 63, unpinned 0                               (F-6)
    C-10 quarantines 0, run_error_lines 0, halted null           (F-7)
    C-3  R1_detected/retained_scored        677 -> 656           (G-4)
    C-3  ps3_8080                             1 -> 2             (G-6, §5)
    C-1  excluded_ps3_8080_total              1 -> 2             (G-6, §5)
    pops/census   rows_exact_mean_pct     96.25 -> 97.19
    pops/enriched rows_exact_mean_pct     96.18 -> 96.84

The one A-only leaf and two of the B-only leaves are a `FUNCTIONAL` verdict
moving between two enriched strata; the rest are the new discard's record and
the three archive renames `C-6` records as tree state.

---

## 9. **THE CLOSING CONTROL — THE FIDELITY CHECK WITH THE ERA GUARD ON**

`python3 sw/fz2_replay.py --all-failures --pass-sample 150 --leg ret --jobs 4`,
against the **newly derived** ledger, **no `--no-fabric-era-guard`**:

    FABRIC ERA GUARD: PASS
      bitstream 2dc38a17d6c3e77d...  flashed 2026-08-10T17:56:19Z  git 77838ef777
      receipt   c0ed51936233ee24...  fz2 FLASH#15 RETENTION ret_draw2
      its inputs 87/88 hash IDENTICAL in the tree at HEAD
        MOVED    hdl/nec_test_ucore.qsf  [EXEMPT -- Quartus rewrites it, carries no RTL]

    population 266 seeds with banked socket rows (116 fabric-FAIL, 150 fabric-PASS)
    DUT tb_sys hdl/tb/obj_dir_sys_ret/Vtb_sys   receipt d039f58ab9f830af...

                       replay PASS   replay FAIL
      fabric PASS              150             0
      fabric FAIL                0           116
      AGREEMENT 266 / 266 = 100.0 %
      of the 116 both-FAIL seeds, first_bad IDENTICAL on 116

**100 % in every cell of every decomposition** — by wait mode
(fixed 59/59, wrand 45/45, wvec 12/12), by whether the seed carries a stimulus
event (48/48 and 68/68, the one-scheduler blindness that motivated the
instrument), and by all fifteen ledger families.

**This is the statement the whole wave's attribution rested on and it is now
made on shared silicon rather than across an era gap.**  The `P2` sitting's
19-seat prediction was taken with `--no-fabric-era-guard` on both legs because
the board carried neither tree; that override is no longer needed and was not
used.  `tb_sys` receipt `d039f58ab9f830af…` is **the same binary the prediction
was computed on**, so this is the same instrument re-read against a board that
has caught up with it.

---

## 10. BOARD HYGIENE

| | |
|---|---|
| `div_guard` total | **63** — control 10 + preflight 2 + capture 48 + `506039` probe 2 + idle 1 |
| **UNPINNED** | **0** |
| `RigMismatch` / transport errors | **0 / 0** |
| quarantines / run-error lines | **0 / 0** (bar C-10) |
| short strata | **0** |
| `check_ab_hw chip 800` after everything | **MATCH over 800 rows** |
| `board_idle()` | **OK, `use_core = 0` left selected** |
| socket only | `use_core=False` explicit on every leg |

`fz2_idle.json` records `board_procs_after` containing the transport's own
`v30ctl.py serve`, the connection this session opened — the same row every prior
sitting has recorded.

---

## 11. WHAT THIS SITTING ESTABLISHES, AND WHAT IT DOES NOT

**ESTABLISHES.**

* **Both landings are in silicon and both work, seat for seat.**  29 of 29
  registered seats closed on a bitstream that had never carried either, with
  **zero losses, zero unregistered closures, and zero unregistered
  first-divergence decreases.**
* **`P1`'s "one arbiter, one rail" is right in fabric**: all ten of its seats
  closed, and its two registered non-closers stayed open at **exactly** the row
  it predicted, which is its §2.2 EU-post-timing residual confirmed rather than
  argued.
* **`P2`'s two laws are right in fabric**, and its §5A.2 finding survives
  contact with silicon: **`M3` is not a separate mechanism.**  Both of its
  seats closed with no `M3` term in the tree.
* **`fz2_replay` is a faithful instrument on its own era**: 266/266 and
  116/116 `first_bad_row`, era guard passing by arithmetic.
* **The offline-instrument gap FLASH #14 measured is closed.**  F14's ten
  unregistered closures were the one-scheduler harness's blindness; this
  sitting predicted the exit set exactly and it was exact.

**DOES NOT ESTABLISH.**

* **That nothing was lost.**  G-6 missed, C-CHIP missed as registered, and
  `fz2e/506039` left the matched set by becoming a discard.  §5 measures it as a
  one-off capture five times over, and **the corpus's capture-noise floor has
  never been measured** — that is an open population with a falsifier, not a
  closed question.
* **Any mechanism for the 116 survivors.**  `E1` (41), the `D*` families (30)
  and `A3` (15) are untouched and unexplained, exactly as they were.  The two
  `B2` survivors are `P2` §2.2's one prefetch (blocked on a `v30u_biu.sv`
  port-list split, booked with a falsifier); `fz2c/407065` and `fz2e/523045` are
  `P1` §2.2's EU post timing, likewise booked; `fz2c/407064` is family `B1`'s.
* **Any Fmax closure.**  Two identical draws are not a distribution, and the
  retention-vs-control inversion is now three for three and still unexplained.
* **That the arch-convergence reading is a law.**  17 of 17 converged here and
  14 of 14 at FLASH #14; that is a precedent over two sittings and no more.
* Nothing about `sim/`, `wr1`, `b2`, or the four `_v1_anchor_stop` timed gates —
  all still `GEN_DRIFT` or anchor-broken on this branch.

---

## 12. TREE STATE AND ARTIFACTS

* Branch `fuzz-v2-on-relanding`.  `hdl/rtl/` untouched by this sitting.
* **New ledger** `sw/testdata/fz2/fz2_failure_ledger_f15_2026-08-10.json`;
  `fz2_ledger.CURRENT` moved to it, which is the single place every consumer
  reads.  **The FLASH #14 ledger is left byte-identical.**
* **Archived by rename, nothing deleted**: `fz2c-F14-archive`,
  `fz2e-F14-archive`, and `fz2_{capture,bars,control,preflight}_F14-archive.json`.
  ⚠ **A BOOKKEEPING NOTE, RECORDED RATHER THAN AMENDED.**  The four `.json`
  renames were staged with `git mv` during the sitting and were therefore
  carried into commit **`af2a00b3f9`** — the scorer commit — whose message
  describes only the scorer.  Nothing is lost and nothing is misdated; the
  renames are visible as `R100` in `git show --name-status af2a00b3f9`.  It is
  stated here because the commit message does not state it, and **the commit is
  not amended**.
* `sw/testdata/fz2/fz2_replay_fidelity_f15_2026-08-10.json` — §9's artifact.
* `sw/testdata/flash_log.jsonl` — **18 entries**, tail = FLASH #15.
* ⚠ **A CONCURRENT OFFLINE WRITER WAS ACTIVE ON THIS TREE AND IT IS RECORDED
  RATHER THAN LEFT FOR A REVIEWER TO NOTICE.**  Commit **`915af531a1`**
  (`fz2_failview`, the failure atlas) landed at 11:28 local, between this
  sitting's scorer commit and its results commit, and it reads this sitting's
  new ledger (its "VALIDATED 116/116" is the F15 failure count).  It is **20
  files, ALL ADDITIONS** — `sw/fz2_failview.py`, `docs/notes/fz2_atlas/*` and
  one coverage JSON — and it **modifies nothing**: not the corpus, not the
  ledger, not the receipts, not `hdl/rtl/`, and not the board.  The BOARD
  single-writer check that governs every figure here is `fz2_w1 preflight`'s
  own (`no v30/serve process on the board -> SINGLE WRITER`, run after the
  flash) and it passed, as did `C-10` (0 quarantines, 0 run-error lines) and
  `C-9` (0 errors).  **Nothing in this document is contaminated by it**, but
  "single writer" was true of the board and not of the checkout, and that
  distinction is worth stating out loud.
* Quartus receipts `7bb5d317cf3ff6a2…` / `c0ed51936233ee24…`; DUT receipts
  `Vtb_v30_core 1f7af54a0174cede…`, `Vtb_sys(ret) d039f58ab9f830af…`.
