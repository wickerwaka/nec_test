# FLASH #16 — THE WAVE-3 MEASUREMENT SITTING FOR `P5′`
# PRE-REGISTRATION, committed BEFORE any board contact

Branch `fuzz-v2-on-relanding`, HEAD **`138fe6e1a2`**.  The board carries
**FLASH #15** (`2dc38a17d6c3e77d…`, flashed 2026-08-10T17:56:19Z from
`77838ef777`), which **predates this wave's one RTL landing**.  Under the
standing rule **no fabric figure taken on FLASH #15 may be quoted against this
tree**, and nothing below is a fabric claim until FLASH #16 is verified on the
board.

The one landing:

| # | landing | commit | RTL touched |
|---|---|---|---|
| 1 | **P5′** — the CS-retarget precharged-read law: `cmt_cs_live = flush_cs & flush_cs_old`, a wired AND on all 16 bits that DELETES a fitted one-bit special case | `d1b3b404bf` (merged `5f3165084a`) | `v30u_biu.sv` (one expression) |

**Verified before writing this**: `git diff 24ea7eb580 138fe6e1a2 -- hdl/rtl/`
touches exactly **one file**, `v30u_biu.sv`, and its only substantive hunk is
the P5′ expression plus its comment/falsifier block.  **The bitstream this
sitting builds is FLASH #15's RTL plus P5′ and nothing else.**

This is a **small, mostly diagnostic wave.**  P4′, P3/M4 and M10 were surveyed
and **stopped-and-booked** to `v30u_eu.sv` territory (`fz2_p4p5_prereg` §1,
`fz2_m10_diagnosis`); none is in this bitstream.  **The value of this sitting is
the wave-4 map, not the seat count.**  The entire fabric expectation is two
seats.

---

## 0. WHAT THIS SITTING PREDICTS, IN ONE PARAGRAPH

P5′ closes **exactly two seats — `fz2c/406023` and `fz2e/527051`**, both ledger
family **D3 both fetched, different address** (mechanism census **M5**), the
`8E /1` (`MOV CS,rm`) single-clock retargets.  On the faithful `tb_sys` replay
(`fz2_p4p5_results` §3) they went **8 → 0** and **1011 → 0** with **0 LOST over
654 replayable seeds**.  Everything else is a **null**: the four D3 stall seats
`(a)` stay failing (their row counts may move — the RTL changes `cmt_cs_live`
for every retargeting D3 seat — but their MEMBERSHIP does not), the other four
D3 seats do not move, the §64.1 four and the C2a four and the `M9` bus-slot
population do not move, `fz2c/404040` does not appear, and no matched seed
becomes a failure.  **The corpus goes 3,722 → 3,724 of 3,838; failures 116 →
114.**  In a mostly no-op wave the NULL is the main content and it is registered
as the heart of the sitting.

---

## 1. THE BUILD — G6, RETENTION CONFIGURATION (`E-*`)

Manual flow, because `quartus_gate.py`'s `build()` runs `quartus_sh --flow
compile` with no `--verilog_macro` and structurally cannot produce a retention
build.  Per draw, from deleted `db` / `incremental_db` / `output_files_ucore`:

    quartus_map --verilog_macro="X1_AD_RETENTION=1" nec_test -c nec_test_ucore
    quartus_fit  nec_test -c nec_test_ucore
    quartus_asm  nec_test -c nec_test_ucore
    quartus_sta  nec_test -c nec_test_ucore
    python3 sw/quartus_gate.py --parse-only --no-qsf-check --label "F16 … RETENTION draw N of 2"

| # | bar | registered |
|---|---|---|
| **E-1** | `gen_ucore_qsf.py --check` **BEFORE** each draw | PASS (measured PASS at HEAD at registration) |
| **E-2** | 0 errors, every stage `Successful`, 0 latches, 0 `lpm_divide` | both draws |
| **E-3** | `divclk` Fmax | **≥ 32 MHz**, both draws |
| **E-4** | worst setup slack | **> 0 ns**, both draws |
| **E-5** | TNS setup **AND** hold | **0.000 on every domain**, both draws |
| **E-6** | the receipt **self-labels RETENTION** | `configuration` begins `RETENTION (X1_AD_RETENTION=1) -- DERIVED from …flow.rpt, …map.rpt`, `configuration_detail.retention == true` |
| **E-7** | the two draws agree | identical Fmax / worst setup / TNS / ALMs and an identical 88-file input manifest; the `.rbf` sha256 is the bitstream identity and is expected identical (the `.sof` is stamped and may differ) |
| **E-8** | `gen_ucore_qsf.py` regenerated and re-checked **AFTER** each draw | PASS |
| **E-9** | the macro's effect is CHECKED, not asserted | the retention `.rbf` **differs** from this tree's CONTROL `.rbf` `99a32fac135d40d8…` |

**E-6 is a HARD STOP if it reads `CONTROL/DEFAULT`** (the `4bb65d2ab6` defect);
the bitstream may not be flashed on it.

### 1.1 THE RETENTION-VS-CONTROL SIGN — REGISTERED AS AN OBSERVATION, NOT A BAR

This tree's CONTROL is **40.42 MHz / +6.509 ns**, two identical draws
(`fz2_p4p5_results` §3.2).  On this branch retention has drawn **ABOVE** control
three times running (FLASH #13 39.37 → 39.83, #14 39.47 → 40.97, #15 40.00 →
42.24), inverting the historical sign.  **I register NO Fmax point estimate and
no band.**  `standing_gates.md` §A governs — the same tree has drawn 19.42 and
45.91 MHz.  **What is registered is the reporting obligation**: this sitting
will state the sign of (retention − control) explicitly and it will be the
**fourth** observation in the series regardless of which way it falls.  If the
sign inverts a fourth time the effect is real and belongs to the fit's seed
distribution rather than the retention model (A-1.1a's registered falsifier).

---

## 2. THE FLASH AND FIRST LIGHT (`F-*`)

| # | bar | registered |
|---|---|---|
| **F-1** | `sw/safe_flash.sh <ucore .sof>` — PREP / `quartus_pgm` / VERIFY | VERIFY OK; `sw/testdata/flash_log.jsonl` **18 → 19 entries**, tail `verify: "OK"` |
| **F-2** | first light `check_ab_hw.py all 800` | **MATCH over 800 rows on all three legs** (chip-vs-golden, core-vs-chip, core-vs-golden), `rc 0` |
| **F-3** | RBCHECK (`fz2_w1.py control`) | **exactly 8 registers** round-trip: `EVT_ADDR[0..2]`, `EVT_CFG[0..2]`, `TVEC`, `VECCTL` |
| **F-4** | C-6 control, `fz2_w1.py control` | **9 legs / 51 checks / 51 PASS**, `holds_proved [2, 20, 300]`, `pins_proved [pin_int, pin_nmi]`, `tvecs_proved [[0,48896],[3056,8]]`, P1–P5 run lengths **2 · 300 · 2 · 300 · 20** to the clock, N1 negative control PASS |
| **F-5** | `use_core=0` chip proof **after everything** | `check_ab_hw.py chip 800` → **MATCH over 800 rows** |
| **F-6** | `div_guard()` tally | **0 UNPINNED** — that is the bar; the count itself is reported, not barred |
| **F-7** | transport | **0 `RigMismatch`**, 0 transport errors |
| **F-8** | `board_idle()` | clean, `use_core=0` left selected |

**F-2 justification, registered in advance.**  P5′ needs a CS-write row (`8E /1`
`MOV CS,rm`) meeting a standing fetch announcement.  The boot program contains
none — no `MOV CS,rm` at a retarget boundary — so the landing is **not reachable
by it** and MATCH 800 is the correct prediction.  Any deviation is a hard stop.

**F-4 IS SCORED ON ITS VERDICTS, NOT ON ROW SHA256** (two `control` runs on the
same flash give differing row sha256s and 51/51 both times).  Row-sha movement
is **not** a stop.

---

## 3. THE CORPUS RE-CAPTURE (`G-*`)

Archive by rename first — the SUP discipline, **nothing deleted**:

    sw/testdata/campaigns/fz2c          -> fz2c-F15-archive
    sw/testdata/campaigns/fz2e          -> fz2e-F15-archive
    sw/testdata/fz2/fz2_capture.json    -> fz2_capture_F15-archive.json
    sw/testdata/fz2/fz2_bars.json       -> fz2_bars_F15-archive.json
    sw/testdata/fz2/fz2_preflight.json  -> fz2_preflight_F15-archive.json
    sw/testdata/fz2/fz2_control.json    -> fz2_control_F15-archive.json

then `fuzz_campaign.py new fz2c` / `new fz2e` (re-pins each manifest to the
resident flash), `fz2_w1.py preflight --board`, `fz2_w1.py capture`.

| # | bar | registered |
|---|---|---|
| **G-1** | corpus identity unchanged | `SEED_LIST_SHA256 = 45d25f31a325c496…`, 48 strata, `fz2c` 960 + `fz2e` 2,880 = **3,840** |
| **G-2** | preflight | `verdict: "OK"`, `board_leg: true`, 0 GEN_DRIFT on the regeneration sample |
| **G-3** | capture completeness | **48 / 48 strata**, `rc 0`, no `halted`, `results.jsonl` **960 / 2,880** rows |
| **G-4** | retained captures | **NOT BARRED — reported.**  Retention on the enriched tier is divergence-driven; two seats closing lowers it.  The bar is only that **every seed named in any prediction below is still retained and scored** |
| **G-5** | the flash pin | every result row's `era.sof_sha256` = FLASH #16's `.sof` sha256; `distinct_eras 1`, `absent 0`, `build_stale 0` over 3,840 |
| **G-6** | discards | **2**, `fz2e/506039` **and** `fz2e/509069`, denominator **3,838** — **and this is a prediction, not an assumption** |

**G-6's basis and the re-roll process.**  `_ps3_8080()` is a **SOCKET-leg**
predicate (amendment A-2): a core landing cannot move it by construction.
FLASH #15 measured it True on exactly **2 of 3,840** (`fz2e/506039`,
`fz2e/509069`), both escaped/non-reproducing socket seeds.  **The registered
A-12 / A-13 process is invoked here in advance**: a new or vanished discard is
**itemised loudly, seed by seed, with its `escaped_n`**, the denominator moves
with it, and the rate is reported on BOTH bases.  A discard re-roll is NOT a
hard stop and NOT a ratchet violation.  Note `fz2e/506039` was FLASH #15's one
non-reproducing socket capture (F15 §5.1, 5/5 SUCCESS on probe); if it flips
back to a matched seed the denominator is 3,839 and that is reported, not a
miss.

---

## 4. THE SEAT-LEVEL PREDICTIONS (`P-*`)

**Reference ledger: FLASH #15's** (`fz2_failure_ledger_f15_2026-08-10.json`,
116 failures, corpus 3,838 scoreable, era `.sof 2dc38a17d6c3…`).  Membership is
`bad_rows != 0` (the bus rows); the `arch` field is REPORTED, not a membership
criterion (`fz2_ledger.py` §15).

### 4.1 — **P-1: THE TWO SEATS CLOSE, SEED FOR SEED**

| seat | family | `first_bad_row` | `diverging_rows` F15 → predicted | `arch` |
|---|---|---|---|---|
| `fz2c/406023` | D3 | 1230 | **8 → 0** | OK (a rows-close IS a clean ledger exit) |
| `fz2e/527051` | D3 | 156 | **1011 → 0** | AW,BP,BW,CW,IX,IY,PSW (membership is bus rows; it exits on `bad_rows == 0` regardless of the dump) |

**P-1 REGISTERED: both leave the ledger; failures 116 → 114.**

**P-1a — `fz2e/527051` is the sharper of the two and it is the sitting's
principal uncertainty.**  At FLASH #15's C-CHIP it was **the one seed of 655
that moved on a scored row** (F15 §4.2), a genuine chip mover in both eras.  The
faithful replay closes it 1011 → 0; whether the SILICON closes it is the test.
Registered: **if `fz2e/527051`'s rows do not close on FLASH #16, that is a
fidelity finding** — the faithful `tb_sys` replay disagreed with silicon on the
one seat that carries the wave — **and it is named before any seat claim is
quoted**, per the brief.  `fz2c/406023` (arch OK, rows 8) is the cleaner closer.

### 4.2 — **P-2: THE FOUR D3 STALL `(a)` SEATS STAY FAILING**

P5′ changes `cmt_cs_live` for every retargeting D3 seat, so these four are
DISTURBED — their row counts MAY move — but their MEMBERSHIP does not (owner is
`v30u_eu.sv`, booked in `fz2_p4p5_results` §2.2).  Faithful-replay values:

| seat | `first_bad_row` | `diverging_rows` F15 → faithful-replay after |
|---|---|---|
| `fz2e/520062` | 700 | 43 → **2965** |
| `fz2e/528008` | 628 | 3192 → **3196** |
| `fz2e/532012` | 328 | 4 → **4 (untouched)** |
| `fz2e/533028` | 881 | 2976 → **12** |

**REGISTERED: all four stay in the ledger (family D3).**  Row-count movement is
EXPECTED and reported, not a violation.  **A ledger EXIT from any of the four is
a finding** — the faithful replay kept all four failing and a fabric exit would
say the offline instrument mis-called the stall/retarget split.

### 4.3 — **P-3: THE FROZEN COUNTER-POPULATIONS — ZERO MOVEMENT**

**Registered: none of these exits the ledger, and `first_bad_row` is
UNCHANGED**; small `diverging_rows` moves from downstream noise are reported,
not violations, and a ledger EXIT or a `first_bad_row` change is itemised.

* **The other four D3 seats** (NOT `8E /1` retargets; P5′ does not touch them):
  `fz2c/407036` · `fz2e/520013` · `fz2e/521016` · `fz2e/527008`.
* **The §64.1 counter-population (4)**: `fz2c/406063` · `fz2c/410047` ·
  `fz2e/518053` · `fz2e/535027`.
* **The C2a seats (4)**: `fz2c/405002` · `fz2c/405013` · `fz2c/405072` ·
  `fz2e/512056`.
* **The `M9` bus-slot population (29)**: `fz2c/404049 405025 407000 407067
  408068 409077`, `fz2e/501069 510043 510048 511014 512062 513026 515047 516026
  516066 520005 520013 527008 527017 527065 530017 530020 531030 532000 532021
  532066 534062 535004 535027` (`fz2e/520013`, `fz2e/527008`, `fz2e/535027`
  overlap the D3-other / §64.1 lists; the union is the frozen set).

### 4.4 — **P-4: `fz2c/404040` MUST NOT APPEAR**

The sharpest falsifier this branch has — a banked SUCCESS on FLASH #15 whose
silicon refuted the `C2` sitting's registered form.  P5′ touches only
`cmt_cs_live`.  **If it is a failure on FLASH #16, this landing has broken a
mechanism nobody claimed, and that is a landing-level finding.**

### 4.5 — **P-5: ZERO LOSSES.**  No currently-matched seed may become a failure.
The faithful replay measured **LOST 0** over the 654 replayable seeds.  A new
failure is a finding of the first order, itemised seed by seed with its family,
`first_bad_row` and whether its chip leg moved.

### 4.6 — **P-6: NO UNREGISTERED CLOSURE.**  Exits from the ledger are EXACTLY
the two named in §4.1 — no more and no fewer.  An unregistered closure is an
instrument finding about the faithful replay, and it is reported as one.

### 4.7 — **P-7: NO UNREGISTERED FIRST-DIVERGENCE DECREASE.**  P5′ names no
seed whose `first_bad_row` decreases.  Any `first_bad_row` decrease on a
still-failing seed is a refutation and is itemised.

### 4.8 — **P-8: THE FAMILY TABLE.**  The only family that moves is **D3, 10 →
8**; every other family count is UNCHANGED.

| ledger family | F15 | predicted F16 |
|---|---|---|
| D3 both fetched, different address | 10 | **8** |
| all fifteen others | (unchanged) | (unchanged) |
| **total** | **116** | **114** |

---

## 5. THE REGISTERED HEADLINE

### 5.1 THE PRIMARY

On the FLASH #15 denominator of **3,838** (2 discards):

    SEED MATCH   3,722 + 2 = 3,724 / 3,838  =  97.0297 %
    failures     116 -> 114

### 5.2 THE REGISTERED FLOOR

`fz2c/406023` (arch OK, rows 8) is the certain closer; `fz2e/527051` is the
chip-mover under test (§4.1a).  If only the certain one closes:

    SEED MATCH   3,722 + 1 = 3,723 / 3,838  =  97.0036 %
    failures     116 -> 115

**A result of 3,724 with both D3 single-clock seats closed is P5′ performing as
measured.  A result at 3,723 with `fz2e/527051` still failing is a FIDELITY
FINDING about the faithful replay, reported as registered.  A result below
3,723, or ANY seed leaving the 3,722 not named in §4.1, is a MISS.**

### 5.3 THE ROW FIGURE — DIRECTIONAL, NOT TO THE DIGIT

The two closers delete **1,019** diverging rows (8 + 1011).  The four `(a)`
seats add ~2,922 (`520062`) + 4 (`528008`) − 2,964 (`533028`) + 0 (`532012`) ≈
**−38** on the faithful replay, but their FABRIC row counts are not predicted to
the digit.  **Point estimate ≈ 121,100 diverging rows, ≈ 98.93 % row match**, a
small rise.  §38.4's rule is invoked: SEED and ROW match are both reported, and
only the seed figure is a ratchet.

### 5.4 HOW THE LEDGER IS RE-DERIVED

`python3 sw/fz2_ledger.py` with its **acceptance control run first**:
`--control --suffix=-F13-archive` must reproduce the committed A-15 ledger
(198 failures as a set, 14 family counts, `first_bad_row` 198/198,
`diverging_rows` 198/198, 3 discards, denominator 3,837).  **If the control
does not reproduce, the new ledger is not quotable and the finding is the tool,
not the corpus.**  `--chip-control=-F15-archive` is run as C-CHIP; a failure is
a hard stop for **attribution**, not for the sitting, and a partial failure is
itemised seed by seed (F15 read 654/655 on scored rows, its one mover being
`fz2e/527051` — the very seat P5′ now closes).

### 5.5 THE CLOSING CONTROL — THE FIDELITY CHECK WITHOUT THE ERA OVERRIDE

`python3 sw/fz2_replay.py --all-failures --pass-sample 150 --leg ret` against
the **newly derived** F16 ledger, **era guard ON, no override**.  On FLASH #16
the board carries this tree's RTL, so the guard must pass by arithmetic.

**Registered: the replay agrees with fabric on every seed it scores, and
`first_bad_row` matches on every seed both legs fail** — reconverging to a
100 % verdict as it did at FLASH #15 (266/266).  **A disagreement here is an
instrument finding and is reported before any seat claim is quoted.**  The
failure atlas is regenerated last (`fz2_failview.py --ledger <F16 ledger>`,
`--ledger` passed EXPLICITLY because line 574 hard-codes the F14 default) so the
wave-4 map is current.

---

## 6. WHAT THIS SITTING DOES NOT CLAIM

* **Nothing about `sim/`.**  The C++ model is defunct on this branch.
* **No mechanism claim for the 114 survivors.**  `E1` (41), the `D1/D2` and
  other-`D3` families, `A3` (15), `C2` (10) are untouched and unexplained.
* **No P4′, P3/M4 or M10 landing.**  All three are booked to `v30u_eu.sv` with
  falsifiers (`fz2_p4p5_prereg` §1, `fz2_m10_diagnosis`) and none is in this
  bitstream.
* **No Fmax closure.**  §1.1.
* **No claim that `fz2e/527051`'s arch DUMP is predicted** — only its bus rows.

---

## 7. HARD STOPS — halt and report

1. `safe_flash` **VERIFY failure** → **physical power cycle**, no blind retry,
   no bridge access.
2. Any `div_guard` **UNPINNED** readback.
3. Any **`RigMismatch`**.
4. **5 consecutive quarantines** or any **short stratum** (`written < 80`).
5. Any capture-integrity bar failing (G-1 … G-5).
6. First light below **MATCH 800** on any of the three legs.
7. **E-6**: a retention build whose receipt reads `CONTROL/DEFAULT`.
8. **Any contradiction inside this pre-registration.**

**Not stops**, each reported with its denominator: C-6 row-sha movement; a
discard re-roll (§3, G-6); a retained-capture count that falls (§3, G-4); the
four `(a)` seats' row-count movement (§4.2); a ROW MATCH that falls while SEED
MATCH rises (§5.3); C-CHIP failing partially with its movers itemised (§5.4); a
`fz2e/527051` that does not close (§4.1a, §5.2 — a fidelity finding, not a miss
of the sitting).

---

## 8. TREE STATE AT REGISTRATION

* HEAD **`138fe6e1a2`**, branch `fuzz-v2-on-relanding`, **`hdl/rtl/` clean**.
* `gen_ucore_qsf.py --check`: **PASS** at registration.
* **Single writer**: board `root@mister-nec` reachable and idle (0 users, no
  `v30`/`serve`/`fz2` process); no `quartus`/`verilator`/`v30ctl` running on the
  host when this was written except this sitting's own retention build.
* `sw/testdata/flash_log.jsonl`: **18 entries**, tail = FLASH #15
  (`2dc38a17d6c3e77d…`).
* CONTROL G6 on this tree: **40.42 MHz / +6.509 ns / TNS 0.000 setup AND hold on
  every domain**, two identical draws (`fz2_p4p5_results` §3.2), CONTROL `.rbf`
  **`99a32fac135d40d8…`**.
* `sw/testdata/fz2/fz2_failure_ledger_f15_2026-08-10.json` — left byte-identical;
  the re-derivation writes a NEW file, `fz2_failure_ledger_f16_2026-08-10.json`.
