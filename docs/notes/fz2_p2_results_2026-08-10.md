# WAVE PACKAGE `P2` — RESULTS, AS REGISTERED

Pre-registration: `docs/notes/fz2_p2_prereg_2026-08-10.md`, committed at
**`430a07ebbc`** — **before one line of RTL was edited and before any figure
below was measured**.  Isolated `git worktree`, branch `fuzz-v2-on-relanding`,
base tree **`4e8fa0d160`**.  Offline: no board, no flash.  **`sim/` is not
touched.**

**BOTH REGISTERED LAWS LANDED.  `L-A` CLOSES 7 OF 7 AND `L-B` 6 OF 8; THE TWO
ARE INDEPENDENT AND EXACTLY ADDITIVE; 0 SEEDS ARE LOST OVER ALL 677.  THREE
BARS ARE MISSED AND ALL THREE ARE REPORTED AS REGISTERED — P-2 (6 of 8), P-3
(2 first divergences ONE ROW EARLIER), P-6 (a bar I MIS-DERIVED over the wrong
population).**

---

## 1. THE LANDING

Two files, **three live lines**, nothing else in the tree:

    hdl/rtl/ucore/v30u_eu.sv       comb phase (a), the sample instant
    -   brk_smp_n = q_pop && q_ripe && q_first;
    +   brk_smp_n = (q_pop && q_ripe && q_first) || (bnd_fire && irq_take);

    hdl/rtl/ucore/v30u_eu.sv       the HALT announcement (`eu_halt`)
    -                    !eu_halted;
    +                    !eu_halted && !brk_seen;

    hdl/rtl/ucore/v30u_eu_step.svh S_DECODE2, the HALT park
    -   if (pla3_xop(pv) == PLA3_BL1_HALT) begin
    +   if (pla3_xop(pv) == PLA3_BL1_HALT && !brk_seen) begin

> **`L-A`: A BOUNDARY THAT WALKS THROUGH THE EXTERNAL DOOR STILL SAMPLES THE
> ARM.**  (`C1` said an external recognition does not SPEND the arm; this is
> the same sentence one clause on — *…and it does not SKIP THE SAMPLE either*.)
>
> **`L-B`: `HLT` DOES NOT PARK WHEN IT RETIRES INTO THE TRAP.**

**No flop, no wire, no save-state address, no BIU file, no new opcode named.**
`bnd_fire`, `irq_take` and `brk_seen` are all wires the same blocks already
read, and `PLA3_BL1_HALT` is the test that line was already making.
`v30u_ss_pkg.sv` is byte-identical.

DUT receipts, in the order they were built and scored:

| leg | receipt | what is in it |
|---|---|---|
| BEFORE | `0ff682e0352877e6…` | base tree `4e8fa0d160` |
| `L-A` alone | `a5971538b3b71b80…` | the sample term only |
| `L-B` first form | `810bace691b8ba01…` | the PARK term only — **refuted, §3.1** |
| `L-B` alone | `5401c7b4c2ee2f83…` | park + announcement |
| **AFTER (the landing)** | **`c9a8c2a026aac559…`** | `L-A` + `L-B` |

### 1.1 ONE TERRITORY EXTENSION, DECLARED

The commissioning brief's territory list for `v30u_eu.sv` is *"blocks 284-361,
800-864, 1770-1850, comb phases (a)/(g)"*.  `L-A` is in comb phase (a) and
`L-B`'s park is in `v30u_eu_step.svh`'s allocated range, but **`eu_halt` (the
`assign` at `v30u_eu.sv:2062-2082`) is outside every named block** and this
landing edits it.  Why it had to: §3.1 — `HLT` is split across two clocks BY
DESIGN (S9a: the announcement leads the park by one and rides the opcode's own
pop clock), and with the term at the park alone the law is only half said and
buys NOTHING.  It is not `v30u_biu.sv` (agent A) and not `S_BIND`/`S_PRERD` or
the `wd1`/`row` includes (agent C).

---

## 2. THE RESCORE — **P-1 MET (7 of 7), P-2 MISSED (6 of 8), P-3 MISSED (0 LOST but 2 EARLIER), P-4 MET (5 of 5)**

`sw/fz2_c1_rescore.py`, **677 retained captures**, 0 errors, 0 `GEN_DRIFT`,
both legs in the same isolated worktree, scored against the **FLASH #14**
ledger (prereg §1d).

    row-clean   BEFORE 312 / 677      AFTER 325 / 677
    GAINED (row-clean)                    13
    LOST                                   0
    first divergences moved EARLIER        2
    seeds that became dirty                0
    errors                                 0

**THE THIRTEEN THAT CLOSED, AND NOTHING ELSE IN THE CORPUS MOVED IN EITHER
DIRECTION:**

    class A (L-A)  fz2c/407024  fz2c/409062  fz2c/410062  fz2e/507064
                   fz2e/518046  fz2e/518072  fz2e/519032
    class B (L-B)  fz2c/407057  fz2c/410053  fz2e/521036  fz2e/522071
                   fz2e/529036  fz2e/535042

All thirteen went from **361–2,895 diverging rows to row-clean over their whole
banked window** — not "moved later": each matches the socketed chip cycle for
cycle to the end of the window it was scored on.

**⚠ ROWS ONLY.**  Both rescore instruments compare BUS ROWS.  The banked ledger
also records an architectural dump difference on many of these seats and **this
sitting does not score arch**: the offline TB harness returns rows and the dump
is a board artefact.  A board re-capture is the only thing that can move the
banked corpus headline `3,692 / 3,837`, and none was taken.

### 2.1 **THE TWO LAWS ARE INDEPENDENT AND EXACTLY ADDITIVE — MEASURED, NOT ASSUMED**

Three separate builds, three separate 677-capture legs.  The standing
precedent is explicit that *a bundle's benefit is not evidence for any member
of it* (`CLAUDE.md`, the re-landing campaign's rule), so each half was scored
alone before the pair was scored together:

| leg | row-clean | GAINED | LOST | EARLIER |
|---|---|---|---|---|
| BEFORE | 312 / 677 | — | — | — |
| `L-A` alone | 319 / 677 | **7** — the 7 class-`A` seats and nothing else | 0 | 0 |
| `L-B` alone | 318 / 677 | **6** — the 6 class-`B` seats and nothing else | 0 | 2 |
| **`L-A` + `L-B`** | **325 / 677** | **13 = 7 + 6, seed for seed** | **0** | **2** |

`312 + 7 + 6 = 325`.  **The two seat sets are disjoint, neither law touches the
other's seats, and neither law's gains or losses change when the other is
present.**

### 2.2 P-2 IS A MISS: 6 OF 8, AND THE TWO THAT DID NOT CLOSE ARE **ONE PREFETCH**

`fz2c/406073` and `fz2e/518044` go from **2,116** and **1,117** diverging rows
to **FIVE**, and their first divergence moves **one row EARLIER** (1575 → 1574,
2609 → 2608), which is what fails P-3.  The five rows are one bus cycle,
exactly:

    row 1574  bs PASV!=CODE                 the announcement
    row 1575  bs PASV!=CODE  t Ti!=T1
    row 1576  bs PASV!=CODE  t Ti!=T2
    row 1577                 t Ti!=T3
    row 1578                 t Ti!=T4       ...and nothing after it, to the
                                            end of the 4,000-row window

`fz2e/518044` is the same five rows at 2608-2612.  Read against the chip
(`fz2c/406073`): after the `IRET` redirects to `e0a3c`, whose first byte is
`F4`, the chip fetches `e0a3c` and `e0a3e` and **stops**; the core fetches a
third word at `e0a40`.  Both legs then read vector 1 at the **identical row
1583** and run together to the end.

**THE MECHANISM OF THE REMAINDER IS NAMED AND IT IS BLOCKED ON A TERRITORY
LINE.**  `eu_halt` carries TWO effects into the BIU and they are separate
statements there: `halt_pending` (the HALT DISPLAY, `v30u_biu.sv:1526`) and
`halted` (the PREFETCH SUSPEND, `v30u_biu.sv:2063`).  Silicon's behaviour on
these two seats says **the trap cancels the display and the park but NOT the
suspend** — the `HLT` still stops the prefetcher, and then the trap is taken at
its boundary.  Saying that needs the two effects split, which is a
`v30u_biu.sv` port-list change and **`v30u_biu.sv` is agent A's file in this
round**.  It is BOOKED, not fitted around.
*Falsifier*: a capture in which the chip issues a prefetch between the `F4`
that retires into the trap and the trap's own vector read.

### 2.3 P-4 MET, 5 OF 5 — THE COUNTER-POPULATION IS UNMOVED

    fz2c/406063   first 249 -> 249   bad 3168 -> 3168
    fz2c/410047   first 227 -> 227   bad 3589 -> 3589
    fz2e/518053   first 571 -> 571   bad 3409 -> 3409
    fz2e/535027   first 296 -> 296   bad 3226 -> 3226
    fz2c/404040   first 351 -> 351   bad 1620 -> 1620

Not one row moved on any of the five, on every one of the four legs above.

**⚠ ONE CORRECTION TO THE BRIEF'S WORDING.**  The brief says *"`fz2c/404040`
must stay a PASS"*.  On **this** instrument it is not a pass and never was: its
BEFORE leg is `first 351 / bad 1620`.  It is a PASS on the BOARD (it is not in
the FLASH #14 ledger).  What is measured here is that it does not move, which
is the strongest thing this instrument can say about it.

### 2.4 P-5 MET — `P3` IS NO WORSE, AND ONE `M3` SEAT IMPROVED

All 12 `M4` seats and both `M3` seats keep their offline `first_bad` to the
row.  One improved: `fz2e/531009` (`M3`) goes `bad` **581 → 4** with `first`
unchanged at 2888.  Nothing got worse.  **None of the 14 is SCOREABLE on this
instrument (prereg §1c), so no closure is claimed for any of them.**

---

## 3. THE ONE THING THAT WAS REFUTED, AND ITS OWN FALSIFIER REFUTED IT

### 3.1 `L-B`'s FIRST FORM — THE PARK TERM ALONE — BUYS NOTHING

The pre-registration wrote `L-B` as a single term at `S_DECODE2`.  Built and
scored alone (receipt `810bace691b8ba01…`) that form gives:

    row-clean 312 / 677   GAINED 0   LOST 0   ERRORS 1

**Zero of the eight class-`B` seats moved**, and `fz2c/410053` hit
`v30u_biu.sv:2242`'s `sev bound violated` assertion.  The reason is measured,
not guessed: on `fz2e/535042` the trap now fires at the chip's own row and
pushes the chip's own IP `0e84` — and **the core still drives a HALT cycle at
row 820 that the chip leaves PASV**, because `eu_halt` is computed one clock
EARLIER, off the queue byte at the opcode's pop clock, and knows nothing about
what `S_DECODE2` decided.  A part that announces a halt it then does not enter
is also what put the BIU out of its own bound.

The repair is the same term at the other half of the same act, and it is not a
second mechanism: S9a's own comment says *"HLT IS DECODED, NOT MICROCODED …
`eu_halt` LEADS by one clock … One rule, both paths."*  **The law is one
sentence; the RTL splits the act across two clocks, so the sentence has to be
said at both.**  With both halves in, the same leg gives 6 GAINED, 0 LOST, 0
errors.

This is recorded rather than smoothed because the registered form was wrong and
its own registered bar is what caught it.

---

## 4. THE STANDING GATES — **P-8, P-9, P-10 ALL MET; P-6 MISSED AND THE BAR WAS MINE**

    gen_ucore_qsf --check                                  PASS           MET
    r7_lint                       PASS, 20 nets / 1 carrier / 3 tainted /
                                  51 `stop` sites / 0 violations          MET
    ss_lint --core ucore   PASS, SS_VERSION 0x8D / SS_BIU_COUNT 103 /
                           SS_EU_COUNT 122 / SS_COUNT 226 / SS_TAG 0x8DE2,
                           214 flops, 0 UNMAPPED  -- UNCHANGED             MET
    test_artifact                                        45 / 45          MET
    check_core --core ucore --opcodes all --cases 0 169,000 / 169,000     MET
      ...of which INT.F3AA                                200 / 200       MET
    s10-hltsweep-w0 --waits 0                              97 / 97        MET
    s10-hltsweep-w1 --waits 1                              93 / 95        MET
    s13-hltsweep-w2 --waits 2                              45 / 46        MET
    s13-hltsweep-w3 --waits 3                              44 / 45        MET
                                            sweeps total  279 / 283       MET
    v0.1-w0evt / -w1evt / -w2evt / -w3evt / -w1evt-biased
                                200 / 1,200 / 200 / 1,200 / 1,200         MET
    check_boot --core ucore --timed 220 / --timed 400   MATCH / MATCH     MET
    check_fuzz_bank   PASS, 621 seeds, stable 621 / improved 0 / worse 0 /
                      gen_drift 0 / regen_err 0 / new-sig TIMING 0        (added)
    fz2_w1 lint                PASS, 0 hits, 48 stratum rows              (added)

**THE `check_core` SURFACE IS BIT-IDENTICAL, NOT MERELY NO-WORSE**, and the
four HLT sweeps are unmoved at 279/283 with the same four family-`D` cells —
which matters more here than it did for `C1`, because `L-B` edits the `HLT`
decode itself.  The inertness argument is the same one and its premise is
measured: `C1`'s §4 counted **`PSW.TF` set in 0 of 169,843** golden cases.

**AND IT CUTS BOTH WAYS, SO IT IS REPORTED AS A WEAKNESS: the golden surface
cannot exercise either term AT ALL.**  A 169,000/169,000 is evidence the
landing broke nothing; it is NO evidence that it is right.

### 4.1 P-7 MET — THE DIRECTED CELL IS IDENTICAL, CLAUSE FOR CLAUSE

`sm3_tf_floor_cell.py score --core ucore --floors 4`, on the landed RTL:

    W-0a  TF-clear null: 18 captures, 0 vector-1 entries (bar 0)   MET
    W-0b  clock ruler: 24,372 rows, 0 row-diffs                    MET
    W-1   determinism across 3 repeats: every cell identical       MET
    W-2   surviving floors over 22 cells: [4]                      MET
    W-3   iret=[2,2]  popfnone=[3,3]                               MET
    W-4   traps at the prefix boundary 0; non-start pushed IPs 0   MET
    W-5   storm cadence: 90 pairs, 0 bad                           MET
    W-6   floor 4: 121,860 rows, 0 row-diffs   EXACT               MET

**Every clause is byte-identical to the baseline taken on `4e8fa0d160`.**  This
is the disjoint evidence the pre-registration §5 named: 90 socket captures of a
directed cell, none of them a corpus seed, on which both terms are inert by
construction — so it is a pure NULL and any movement would have been a
refutation.  **`W-0a` in particular is the `M3` over-fire null and it is still
0**: nothing in this landing makes the arm fire where it should not.

⚠ `--floors 4` REWRITES the committed `score-ucore.json` (which holds the 1–7
record).  It was copied out before each run and restored after; `git status`
on `sw/testdata/sm3-s24tfcell/` is clean.

### 4.2 **P-6 MISSED — AND THE BAR WAS MIS-DERIVED BY ME, OVER THE WRONG POPULATION**

Registered: *of the corpus's 101 `has_tf` seeds, 22 have a retained capture and
score 7 row-clean / 15 failing BEFORE; bar row-clean ≥ 20 of 22 AFTER*.

    MEASURED   BEFORE  7 / 22      AFTER  8 / 22      GAINED 1, LOST 0

**Reported as a MISS: 8 of 22 against a bar of 20.**  The bar had no basis —
those 15 failures are 15 different mechanisms and nothing in this package
claimed them.

**AND THE POPULATION IS THE WRONG ONE, WHICH IS THE USEFUL PART.**  `has_tf` is
a GENERATOR property (the seed's program was built with `PSW.TF` planted).  Of
this landing's **15** class-`A`/class-`B` seats, **14 have `has_tf = False` and
`n_ins = 0`** — they are RAW seeds executing open bus, and their `TF` arrives
when an `IRET`/`POPF` pops a random stack word.  Only `fz2e/507064` is in the
`has_tf` population at all, and it is the one seed the tally moved.

**So the `M2` mechanism lives almost entirely OUTSIDE the sub-population the
FLASH #14 results document used to attribute `C1`** (`fz2_f14_results` §5.5's
*"101 TF seeds, 12 failed on #13 and 7 on #14"*).  That reading is not wrong,
but it is narrower than it looks: a `PSW.TF` mechanism is not confined to
`has_tf` seeds, and a future sitting that wants this population should derive
it from the ROWS (a chip vector-1 read below `0x400`), not from the generator
flag.  Registered here as a finding, with no number restated.

---

## 5. G6 — **PASS ON BOTH DRAWS, IDENTICAL; P-11's BAND CLAUSE IS A MISS AND IT IS A MISS UPWARD**

`python3 sw/quartus_gate.py`, CONTROL/DEFAULT configuration (no
`X1_AD_RETENTION`), Quartus 17.1.0 Build 590, each from a clean `db`:

    draw  Fmax     worst setup  TNS           ALMs             receipt
    1     41.48    +7.141 ns    0.000 (all)   12,276 (29 %)    7979a9c67f162b0a…
    2     41.48    +7.141 ns    0.000 (all)   12,276 (29 %)    2317fb33d06de8b8…

0 errors, 0 latches, 0 `lpm_divide` on both.  **The 88-file input manifest is
`aee482e6ef986763…` on BOTH draws**, which is the check that the two builds
were compiled from the same bytes; `git` reads `-dirty` on both only because
this document was being written while they ran, and the manifest is the
provenance that binds.

Against this branch's own CONTROL baseline (**39.37 MHz / +5.853 ns / 12,340
ALMs**, `CLAUDE.md`) the landing is **+2.11 MHz and −64 ALMs**.

**⚠ P-11 REGISTERED "inside the branch's registered 39.2 – 40.1 MHz band" AND
41.48 IS OUTSIDE IT — ABOVE.  REPORTED AS A MISS, AND NOT EXPLAINED.**  Every
E-bar (`E2` errors, `E3` Fmax ≥ 32, `E4` worst setup > 0, `E5` TNS 0.000 setup
AND hold) passes on both draws, and the two draws are identical to the
picosecond — but a band is a bar and this is outside it.  It joins the same
list as FLASH #14's 40.97 (`CLAUDE.md`: *"reported, not explained"*): the branch
has now drawn 39.16 · 39.37 · 39.47 · 39.63 · 39.81 · 40.11 · 40.97 · 41.48
on essentially the same design.

⚠ **One green build is not closure and two identical draws are not a
distribution** — `standing_gates` §A and `ucore_provenance` §74.4 apply
unchanged: the same tree has drawn 19.42 and 45.91 MHz.

---

---

## 5A. ADDENDUM — **THE FAITHFUL INSTRUMENT LANDED ON THE BRANCH WHILE THIS SITTING WAS RUNNING, AND IT SAYS 19 OF THE 145, INCLUDING BOTH `M3` SEATS**

**THE ARTIFACT WINS OVER THE BRIEF, AND OVER MY OWN PRE-REGISTRATION §1c.**
While the gates above were running, `fuzz-v2-on-relanding` moved from
`4e8fa0d160` to `0ed6e53fbe` and `b938bc81b3` landed **`sw/fz2_replay.py` —
the faithful offline replay on `tb_sys`**: three pin-event schedulers, `TVEC`,
`VECCTL`, and the seed's own wait source, i.e. the thing whose absence made
`fz2_c1_rescore` unable to score `M3` and `M4` at all.  This sitting was
REBASED onto it and everything in §1–§5 is unchanged by the rebase (the shared
branch carries **no `hdl/rtl/` change**; the G6 manifest `aee482e6ef986763…`
is the same before and after).  Its own `fz2_c1_rescore` ledger repair is
strictly better than mine and **mine was dropped in the rebase in favour of
it** (`fz2_ledger.CURRENT`, `--ledger`, and every consumer prints the file it
read).  Prereg §1d is therefore *reported and superseded, not fixed twice*.

### 5A.1 THE MEASUREMENT

`python3 sw/fz2_replay.py --all-failures --no-fabric-era-guard`, all **145**
FLASH #14 ledger failures, against the same banked SOCKET rows, two builds of
`tb_sys` from the two trees:

| leg | tb_sys receipt | replay PASS | replay FAIL | verdict agreement | first_bad identical |
|---|---|---|---|---|---|
| BEFORE (`fuzz-v2-on-relanding`'s RTL) | `a6e1465c2badaa67…` | **0** | 145 | **145 / 145 = 100.0 %** | **145 / 145** |
| AFTER (this landing) | `7ae5d342cf7296bf…` | **19** | 126 | 126 / 145 | 123 / 126 |

    GAINED 19   LOST 0   seeds with MORE bad rows 0

**The BEFORE leg is the control that makes the AFTER leg readable: on the base
tree the replay reproduces the fabric verdict on 145 of 145 AND the fabric
`first_bad_row` on 145 of 145.**  Every one of the 19 is therefore attributable
to the three lines of §1 and to nothing else.

**THE NINETEEN** (`bad` is the replay's own count against the socket rows):

    C1 family, 9 of 10   fz2c/407024 538   fz2c/409062 387   fz2c/410062 617
                         fz2e/507064 729   fz2e/518046 361   fz2e/518072 391
                         fz2e/519032 483   fz2e/530030 736   fz2e/531000 2605
    B2 family, 8 of 10   fz2c/407057 2609  fz2c/410000 1139  fz2c/410053 2774
                         fz2e/521036 778   fz2e/522071 694   fz2e/529036 833
                         fz2e/529055 1759  fz2e/535042 2895
    NEW/UNCLASSIFIED, 2  fz2e/517043 619   fz2e/531009 577

all `-> 0`.

### 5A.2 **`M3` CLOSES 2 OF 2 WITH NO `M3` TERM LANDED — SO `M3` WAS NEVER A SEPARATE MECHANISM**

`fz2e/517043` and `fz2e/531009` are the brief's two `M3` over-fires, the ones
it describes as *"the arm survives an external recognition (`C1`'s landed law,
correct) but must be spent by a software-`INT` entry"*, with a proposed term
keyed on the EU-initiated vs pin-initiated distinction.  **No such term is in
this landing.**  Both seats go replay-clean on the sample law and the `HLT`
law alone.

**The `M3` hypothesis is therefore SUPERSEDED, not confirmed and not refuted
by its own falsifier**: what looked like an over-fire that needed a new
spending rule was the SAME missing sample seen from the other side — a core
that never armed at the right boundary arms at a later one instead, and fires
where the chip does not.  **This is what the standing principle predicts and
it is worth saying plainly: the brief asked for three terms and the tree needed
two, because two of the three "mechanisms" were one.**  The `M3` falsifier in
the pre-registration §6 stands as written and is now un-owed.

**`M2` on this instrument is 17 of 20** (9 `C1` + 8 `B2`), against 13 of 20 on
`fz2_c1_rescore`.  The four `fz2_c1_rescore` could not show — `fz2c/410000`,
`fz2e/529055`, `fz2e/530030`, `fz2e/531000` — are exactly the four it declared
`OUT` in the pre-registration §2.3 for the one-scheduler reason, so this is the
same seat set read by an instrument that can see it, not a new claim.

### 5A.3 THE THREE THAT STILL FAIL AND MOVED THEIR FIRST DIVERGENCE

    fz2c/406073  B2  first 1575 -> 1574  bad 2116 -> 5     (§2.2's one prefetch)
    fz2e/518044  B2  first 2609 -> 2608  bad 1117 -> 5     (§2.2's one prefetch)
    fz2c/407064  B1  first 1956 -> 1947  bad 1680 -> 998

The first two are §2.2's pair and the faithful instrument reproduces them
seat for seat and row for row, which is independent confirmation that the
remainder is one prefetch cycle and not an artefact of the `tb_v30_core`
harness.  **`fz2c/407064` is NEW information and it is family `B1`
(HALT-cycle address) — another package's family.**  It is a `HLT` seat, so
`L-B` reaches it: it improves by 682 diverging rows and its first divergence
moves **9 rows earlier**.  Reported, not claimed and not repaired: `B1` is
`sw/b1_rescore.py`'s territory and it is the same `eu_halt` split named in
§2.2.

### 5A.4 WHAT THIS DOES **NOT** SAY

* `--no-fabric-era-guard` was passed on BOTH legs, deliberately and
  identically: the board carries FLASH #14, which is neither of these trees.
  **These are two OFFLINE columns against the same SOCKET rows.  Neither is a
  fabric figure and neither may be quoted as one.**  The socket rows are the
  reference and they do not move between the legs.
* Rows only, as in §2.  `arch_match` is not scored offline.
* The corpus headline `3,692 / 3,837` is a BOARD number and is NOT moved by
  this document.  What §5A.1 licenses is a PREDICTION for the next flash — **19
  of the 145 ledger failures, named above, seed for seed** — and that
  prediction is registered here, before any board contact.
* **The §1–§5 numbers are NOT restated.**  `fz2_c1_rescore` measured 13 of 20
  and that is what it measured; §5A is a second instrument on a wider
  population, not a correction of the first.  Both were re-run on
  INDEPENDENTLY REBUILT binaries after the rebase and reproduced exactly
  (`fz2_c1_rescore` **325 / 677** on receipt `378ebda46900b26c…`;
  `fz2_replay` **19 / 145** on `7ae5d342cf7296bf…`).

---

## 6. WHAT THIS SITTING DID NOT DO

* **`M3` WAS BOOKED AND IS NOW SUPERSEDED, NOT LANDED.**  No `M3` term is in
  this landing, and §5A.2 measures both of its seats going replay-clean on the
  two laws that ARE landed.  The proposed EU-vs-pin spending rule is not
  needed and should not be written.  `W-0a` is the standing over-fire null and
  it is 0.
* **`P3` / `M4` IS NOT OPENED, AND THE REASON CHANGED UNDER IT.**  The
  pre-registration said it has no offline benefit instrument; `b938bc81b3`
  landed one on this branch mid-sitting (§5A).  On that instrument all **12**
  `M4` seats still fail BEFORE and AFTER, unchanged — **`M4` is untouched by
  these two laws, which is the cleanest possible statement that it is a
  different mechanism.**  It is now SCOREABLE and un-blocked: a `P3` sitting
  should pre-register against `fz2_replay`, not against `fz2_c1_rescore`.
* **No board contact, no flash.**  The board carries FLASH #14, which predates
  this landing; **no fabric figure in this repo may be quoted against this
  tree.**
* **`BRK_FLOOR`, `brk_p` and the `iret`/`popfnone` asymmetry are untouched**,
  and §4.1 measures that they did not move.
* `sim/` untouched.  `nec_test.sdc` not widened, no `r7_lint` exception added.
* **`sw/b1_rescore.py:51` still hard-codes the FLASH #13 ledger** and is left
  that way — it is another package's instrument.  The defect is real and is
  described in the pre-registration §1d; whoever owns `b1` should take it.
* **`sw/testdata/campaigns/*/captures/` is `.gitignore`d working data and was
  SYMLINKED into this worktree from the shared checkout.**  Nothing was written
  through the links.  A reviewer re-running this needs the same 677 captures.
