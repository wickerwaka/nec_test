# FLASH #16 — RESULTS, AS REGISTERED

Pre-registration `docs/notes/fz2_f16_prereg_2026-08-10.md` (**`ddaed64457`**,
committed before any board contact), scorer `sw/fz2_f16_score.py` (same commit,
written while the retention build was still running and before the F16 ledger
existed).

Every bar is reported in the form it was registered in.  **Nothing is
re-registered after the fact.**

---

## 0. THE HEADLINE, IN ONE PARAGRAPH

**The wave's one RTL prediction is CONFIRMED IN SILICON, and the corpus figure
netted flat because of socket-capture noise the landing cannot touch.**  P5′
closes **exactly the two seats it was registered to** — `fz2c/406023` (8 → 0)
and `fz2e/527051` (1011 → 0), both **in fabric**, seed for seed — and the four
D3 stall `(a)` seats stayed failing with their row counts moving **to the row
the faithful replay predicted**.  P-2, P-3, P-4, P-6, P-7 and the D3 half of P-8
are all **MET**.  The headline **MISSED its floor by one** — **3,722 / 3,838,
not the registered 3,724** — because **two seeds that were matched at FLASH #15
entered the ledger as socket-capture artifacts** (`fz2e/513017`, `fz2e/534041`),
exactly offsetting the two that closed.  **Both are proven to be the physical
NEC V30 socket, not the ucore**: `fz2e/513017` re-captures **5 / 5 to the good
FLASH #15 answer** (the F16 banked row is a one-off, its terminator misfired),
and `fz2e/534041` is an escaped seed running open bus.  A core-RTL landing
**cannot** move the `use_core=0` socket leg by construction, and both seeds'
CORE dumps are unchanged.  The discard set also re-rolled inside the same
population (`fz2e/506039` flipped back to matched, `fz2e/535075` became a new
`ps3_8080` escape).  **The closing control reconverged 266 / 266 = 100 %**, and
`fz2c/404040`, the branch's sharpest falsifier, stayed absent.  This wave
**measures the capture-noise floor FLASH #15 §5.2 registered as never having
been measured**: ≈ 4 socket seeds of 3,840 moved between two flashes, none of
them the core.

---

## 1. THE BUILD — G6, RETENTION, TWO DRAWS (`E-1` … `E-9`)

Both draws, manual retention flow, each from a deleted `db` / `incremental_db` /
`output_files_ucore`, Quartus 17.1.0 Build 590:

| draw | verdict | Fmax (`divclk`) | worst setup | TNS setup/hold | ALMs | receipt |
|---|---|---|---|---|---|---|
| 1 | **PASS** | **40.54 MHz** | **+6.584 ns** | **0.000 / 0.000, every domain** | 12,274 / 41,910 (29 %) | `fc3cb5af7327263f…` |
| 2 | **PASS** | **40.54 MHz** | **+6.584 ns** | **0.000 / 0.000, every domain** | 12,274 / 41,910 (29 %) | `f88783b66cf4a1ee…` |

| # | bar | verdict |
|---|---|---|
| E-1 | `gen_ucore_qsf --check` before each draw | **MET** |
| E-2 | 0 errors, all stages Successful, 0 latches, 0 `lpm_divide` | **MET** |
| E-3 | Fmax ≥ 32 MHz | **MET** (40.54) |
| E-4 | worst setup > 0 | **MET** (+6.584 ns) |
| E-5 | TNS 0.000 setup **and** hold, every domain | **MET** |
| E-6 | receipt self-labels RETENTION | **MET** — `RETENTION (X1_AD_RETENTION=1) -- DERIVED from …flow.rpt, …map.rpt`, both |
| E-7 | the two draws agree | **MET, strongest form** — identical Fmax / setup / TNS / ALMs, identical 88-file input manifest `4ae89fb63557a1a0…`, **`.rbf` BYTE-IDENTICAL `b7e37ea924add8be…`** on both |
| E-8 | `.qsf` regenerated + re-checked after each draw | **MET** — `QSF_RECHECK_RC=0` both |
| E-9 | the macro's effect is checked, not asserted | **MET** — retention `.rbf b7e37ea9…` **differs** from CONTROL `.rbf 99a32fac135d40d8…` |

Draw 2's `.sof` `fed558c0e61173ae…` is what was flashed (the `.sof` differs
between draws because Quartus stamps it; the `.rbf` is the bitstream identity
and it matches).

### 1.1 **THE SIGN ANOMALY WENT THE SAME WAY A FOURTH TIME — REPORTED, NOT EXPLAINED**

    CONTROL     40.42 MHz   +6.509 ns    (two identical draws, fz2_p4p5_results §3.2)
    RETENTION   40.54 MHz   +6.584 ns    (two identical draws, this sitting)
    difference  +0.12 MHz, +0.075 ns  -- RETENTION ABOVE CONTROL

**Fourth consecutive inversion on this branch** (FLASH #13 +0.46, #14 +1.50, #15
+2.24, #16 +0.12).  A-1.1a's registered falsifier now has four of four.
**No Fmax claim and no band; `standing_gates.md` §A governs** — the same tree
has drawn 19.42 and 45.91 MHz, and two identical draws are not a distribution.

---

## 2. THE FLASH AND FIRST LIGHT (`F-1` … `F-8`)

**FLASH #16 is on the board.**  `nec_test_ucore.sof`
**`fed558c0e61173aeb2e6891eda3f16bb48bcb15ebb9fd40496ffbebe0059e66c`**, `.rbf`
**`b7e37ea924add8be…`**, built from **`138fe6e1a2`** (receipt commit
`ddaed64457`) WITH `X1_AD_RETENTION=1` (draw 2), through `sw/safe_flash.sh`
with its VERIFY leg.  **IT IS THE FIRST BITSTREAM TO CARRY P5′.**

| # | bar | measured | verdict |
|---|---|---|---|
| F-1 | flash + ledger, VERIFY OK, log 18 → 19 | **VERIFY ok try 1** (`pwr_good True`, `cpu_running True`, `use_core False`), **19 entries** | **MET** |
| F-2 | first light MATCH 800 on all three legs | **chip-vs-golden · core-vs-chip · core-vs-golden all MATCH 800**, `rc 0` | **MET** |
| F-3 | RBCHECK exactly 8 registers | **8** — `EVT_ADDR[0..2]`, `EVT_CFG[0..2]`, `TVEC`, `VECCTL` | **MET** |
| F-4 | C-6 control, 9 legs / 51 checks | **9 legs, 51 checks, `failing_legs []`**, INTA vector **0xFF**, holds `[2,20,300]` on `[pin_int,pin_nmi]`, TVECs `(0,48896)` / `(3056,8)`, P1–P5 run lengths 2·300·2·300·20, 10 div guards 0 unpinned | **MET** |
| F-5 | `use_core=0` chip proof after everything | **MATCH over 800 rows** | **MET** |
| F-6 | `div_guard` tally, 0 UNPINNED | **0 UNPINNED** across capture (C-8: 63), control (10), preflight, and the reprobe (2) | **MET** |
| F-7 | transport, 0 `RigMismatch` | **0** (C-10: 0 quarantines, 0 run-error lines) | **MET** |
| F-8 | `board_idle()` | **OK, `use_core = 0` left selected** | **MET** |

**F-2's registered justification held**: P5′ needs `MOV CS,rm` at a retarget
boundary; the boot program has none, so MATCH 800 was the correct prediction.

---

## 3. THE RE-CAPTURE AND THE INTEGRITY BARS (`G-1` … `G-6`)

The FLASH #15 corpus was archived **by rename** first — nothing deleted:
`fz2c`/`fz2e` → `*-F15-archive`, and `fz2_capture`/`fz2_bars`/`fz2_preflight`
`.json` → `*_F15-archive.json`.  (`fz2_control.json` was NOT archived as F15: the
C-6 leg had already run on FLASH #16, so the live file is the F16 control; it is
also copied to `fz2_control_f16.json`.  Deviation from §3's list, stated.)
`fz2_w1 bars` scores C-1 … C-11 = **11 / 11 MET**.

| # | bar | measured | verdict |
|---|---|---|---|
| G-1 | corpus identity | `SEED_LIST_SHA256 45d25f31a325c496…`, 960 + 2,880 = **3,840**, 48 strata (C-11 census 480/480 exact) | **MET** |
| G-2 | preflight | `verdict OK`, board leg run, era pinned to receipt `f88783b66cf4…`, **192 seeds hits = 0** (C-5 `gen_drift 0`) | **MET** |
| G-3 | completeness | **48/48 strata**, `rc 0`, `halted null` (C-10), **960 / 2,880** rows | **MET** |
| G-4 | retained captures | **not barred** — reported; every predicted seed retained and scored | **as registered** |
| G-5 | flash pin | **`distinct_eras 1`, `absent 0`, `incomplete 0`, `build_stale 0`** over 3,840 (C-4), every row `fed558c0e611…` | **MET** |
| G-6 | discards | **2**, denominator **3,838** — count MET; **the SET re-rolled** (§3.1) | **count MET, set re-rolled** |

### 3.1 **G-6 — THE DISCARD SET RE-ROLLED INSIDE THE CAPTURE-NOISE POPULATION**

Registered `2` = `{fz2e/506039, fz2e/509069}`.  **Measured `2` =
`{fz2e/509069, fz2e/535075}`.**  The count and denominator (3,838) are as
registered; the SET moved by two, and the A-12/A-13 process is followed —
itemised, both bases, not a hard stop:

* **`fz2e/506039` LEFT the discard set** — FLASH #15's one proven-non-reproducing
  socket capture (F15 §5.1, `ps3_8080` True there, SUCCESS 5/5 on probe).  At
  FLASH #16 its socket leg reproduced the GOOD trajectory, `ps3_8080` went
  False, and it is a **matched seed** again.
* **`fz2e/535075` ENTERED the discard set** — a new runtime-8080 escape,
  `ps3_8080` True, out of the denominator by A-12.

Both moves are the SAME escaped / non-reproducing socket population as §5's
new failures, and **`_ps3_8080` is a SOCKET-leg predicate a core landing cannot
move by construction** (amendment A-2).

---

## 4. THE ACCEPTANCE CONTROLS

**`C-LEDGER` — PASS, 9 / 9** (`fz2_ledger.py --control --suffix=-F13-archive`):
failure SET identical `198/198` symdiff empty, denominator 3,837, matched 3,639,
3 discards, 14 family counts identical, `first_bad_row 198/198`,
`diverging_rows 198/198`, arch `196/198` (the two known OK/NODUMP residuals),
overlay 40.  **The derivation is quotable.**

---

## 5. THE SEAT-LEVEL SCORING — `sw/fz2_f16_score.py`

`python3 sw/fz2_f16_score.py --new sw/testdata/fz2/fz2_failure_ledger_f16_2026-08-10.json`

### 5.1 **P-1: BOTH SEATS CLOSED IN FABRIC, SEED FOR SEED — THE WAVE'S POINT**

| seat | family | F15 → F16 | verdict |
|---|---|---|---|
| `fz2c/406023` | D3 | rows **8 → 0** (arch OK, clean exit) | **MET** |
| `fz2e/527051` | D3 | rows **1011 → 0** (the chip-mover-under-test) | **MET** |

**`fz2e/527051` was FLASH #15's ONE scored-row chip mover** (F15 §4.2); the
faithful replay closed it and **silicon closed it too**.  P5′'s 16-bit wired-AND
is confirmed in fabric on both seats.

### 5.2 **P-2: THE FOUR D3 STALL `(a)` SEATS STAYED FAILING, ROWS TO THE PREDICTED VALUE**

| seat | rows F15 → F16 (fabric) | faithful-replay predicted |
|---|---|---|
| `fz2e/520062` | 43 → **2966** | ~2965 |
| `fz2e/528008` | 3192 → **3197** | ~3196 |
| `fz2e/532012` | 4 → **4 (unmoved)** | ~4 |
| `fz2e/533028` | 2976 → **12** | ~12 |

All four still failing (family D3), row counts moving as the offline instrument
said — **MET**.  This is the disturbance P5′ was registered to cause on the
seats it does not close.

### 5.3 THE REST OF THE NULL

| bar | result |
|---|---|
| **P-3** frozen counter-populations (38: other-D3 + §64.1 + C2a + M9) | **MET** — 0 exits, 0 `first_bad` moves |
| **P-4** `fz2c/404040` must not appear | **MET** — ABSENT |
| **P-6** exits are EXACTLY the two closers | **MET** — 2 exits, 0 unregistered |
| **P-7** no unregistered first-divergence decrease | **MET** — 0 |
| **P-8** family table: only D3 moves 10 → 8 | **D3 MET (10 → 8)**; **NEW/UNCLASSIFIED 1 → 3** — the two §5 socket artifacts (the third is `fz2e/532032`, the F15 survivor) |

### 5.4 **P-5 — MISSED: TWO SOCKET-CAPTURE ARTIFACTS ENTERED, AND P5′ IS EXONERATED**

Two seeds matched at FLASH #15 are failures at FLASH #16.  Neither is D3;
P5′ names neither; P5′'s offline control had **0 LOST over 654 replayable
seeds**; and **a core-RTL landing cannot move the `use_core=0` socket leg**.
Itemised, seed by seed, with the reproducibility probe `sw/fz2_f16_reprobe.py`
(5 fresh captures each, isolated, banked corpus untouched, `div_guard` both
sides, 0 unpinned):

**`fz2e/513017`** (`enr/soup/stim/wrand1`) — **a proven one-off, the FLASH #15
`fz2e/506039` signature exactly**:

| | FLASH #15 | FLASH #16 banked | reprobe ×5 (this bitstream) |
|---|---|---|---|
| verdict | SUCCESS | FUNCTIONAL `func:W@45` | — |
| `bad_rows` | 0 | 502 | — |
| CHIP dump AW | 42426 | **29078** | **42426 (5/5)** |
| CORE dump AW | 42426 | **42426 (byte-identical to F15)** | — |
| terminator `fired` | 5 | **0** | — |

The five fresh captures are **byte-identical to each other on the scored rows
(4/4)** and reproduce the GOOD FLASH #15 answer; **the F16 banked row is the
anomaly.  The CORE is provably not involved** — its dump is byte-identical to
FLASH #15.

**`fz2e/534041`** (`enr/raw/stim/wrand15`) — an **escaped seed** (`escaped_n
24`, program `[1091, 49650]`) running **open bus**: verdict SUCCESS →
KNOWN_ACCEPTED, `sub` clean → `open_bus`, `bad_rows 0 → 1402`, **CHIP and CORE
dumps byte-identical between eras**.  Escaped programs have no reproducible bus
(F14 §4); the five fresh captures agree with each other and the arch is stable.

**Disposition (F15 §5.2, invoked):** the banked figure IS the figure — the
corpus is quoted **as captured, 3,722 / 3,838** — and the probe is **NOT**
substituted for the banked row.  Stated plainly for diagnosis only: with
`fz2e/513017` at the value it reproduces 5/5, the corpus reads **3,723 / 3,838**,
the registered FLOOR exactly.

### 5.5 THE REGISTERED HEADLINE

    denominator            3,838        (registered 3,838)  MET
    SEED MATCH   3,722 / 3,838 = 96.9776 %   (F15 3,722 / 3,838)
      registered PRIMARY   3,724        -2  (the two closers, offset by two socket artifacts)
      registered FLOOR     3,723        MISSED by 1
    failures               116          (registered 114)
    ROW  MATCH   11,203,278 / 11,326,293 = 98.9139 %   (F15 98.9216 %)

**Reported as registered as a MISS**: the headline is one below the floor.
**The cause is fully attributed and it is not the landing** — it is the
socket-capture floor, and this sitting measures it.

### 5.6 THE CLOSING CONTROL — **266 / 266 = 100 %**, WITHOUT THE ERA OVERRIDE

`fz2_replay --all-failures --pass-sample 150 --leg ret`, **era guard ON, no
override** (the guard passed by arithmetic; the `tb_sys ret` binary was rebuilt
on the P5′ tree first, receipt `7439e08b0288d63b…`):

    fabric PASS  150   replay PASS 150   replay FAIL   0
    fabric FAIL  116   replay PASS   0   replay FAIL 116
    AGREEMENT 266 / 266 = 100.0 %   first_bad IDENTICAL on 116 / 116

Agreement is 100 % on every wait mode, every stimulus class and **every ledger
family including NEW/UNCLASSIFIED 3/3**.  The offline instrument the wave's
attribution rests on is faithful to the silicon it now shares an era with — and
its FAIL on `fz2e/513017` is the offline core doing the RIGHT thing while the
banked socket row did the anomalous one, which is the §5.4 finding from the
other side.

---

## 6. THE FAILURE ATLAS — REGENERATED FOR WAVE 4

`fz2_failview.py --ledger <F16 ledger> --split docs/notes/fz2_atlas --index
docs/notes/fz2_failure_atlas_2026-08-10.md`: **116 snippets, fork row reproduced
116/116, image ok 116/116, 0 cap errors.**  The wave-4 map is current.  The
largest remaining mechanisms are unchanged and unexplained: `E1` (41), the `D1`
/ `D2` families (20), `A3` (15), `C2` (10), and D3's remaining 8 — of which the
four `(a)` stall seats are booked to `v30u_eu.sv` (`wr_cs1` qualified by the row
committing) with their falsifier in the RTL.

---

## 7. WHAT THIS SITTING FOUND FOR WAVE 4

* **P5′ works in silicon** — the 16-bit wired-AND `cmt_cs_live = flush_cs &
  flush_cs_old` closes both single-clock D3 retargets in fabric, and it DELETES
  a fitted one-bit special case.
* **The capture-noise floor is real and now has a prior**: across FLASH #15 →
  #16, **four socket seeds of 3,840 moved** with the core provably uninvolved —
  `fz2e/513017` (one-off, proven 5/5), `fz2e/534041` (escaped open bus),
  `fz2e/506039` (flipped OUT of discards), `fz2e/535075` (flipped IN).  **F15
  §5.2's falsifier is now partially answered and stays OPEN as a wave-4 item**:
  double-capture the whole 3,840 on one bitstream and count the non-reproducing
  socket seeds.  Every single-capture seed-level claim in this campaign inherits
  this floor.
* **The four D3 `(a)` stall seats** are the next BIU-adjacent landing, and they
  are `v30u_eu.sv` territory (booked, with a falsifier).

---

## 8. HARD STOPS — none fired

`safe_flash` VERIFY ok try 1; 0 `div_guard` UNPINNED; 0 `RigMismatch`; 0
quarantines; every capture-integrity bar (G-1…G-5) met; first light MATCH 800;
E-6 read RETENTION; no contradiction inside the pre-registration.  The P-5 miss
and the G-6 re-roll are **registered non-stops**, reported with their
denominators and diagnosis.
