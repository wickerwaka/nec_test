# FLASH #14 — RESULTS, AS REGISTERED

Pre-registration `docs/notes/fz2_f14_prereg_2026-08-10.md` (`1e2a6e7a96`),
erratum E-1 (`b89f9aa2ea`), addendum A-1 (`12650eb073`) — **all three committed
before board contact**, and E-1 and A-1 before the flash.  G6 receipts committed
at `2fa3dd33b3`, also before the flash.

Every bar is reported in the form it was registered in.  **Nothing is
re-registered after the fact**; where a registered clause missed it is reported
as a MISS with its cause.

---

## 1. THE BUILD — G6, RETENTION, TWO DRAWS (`E-1` … `E-8`)

Manual flow (`quartus_map --verilog_macro=X1_AD_RETENTION=1` → `fit` → `asm` →
`sta`, then `quartus_gate.py --parse-only --no-qsf-check`), each draw from a
deleted `db` / `incremental_db` / `output_files_ucore`.

| draw | verdict | Fmax (`divclk`) | worst setup | TNS setup/hold | ALMs | err/latch/`lpm_divide` | receipt |
|---|---|---|---|---|---|---|---|
| 1 | **PASS** | **40.97 MHz** | **+6.843 ns** | **0.000 / 0.000, every domain** | 12,256 / 41,910 (29 %) | 0 / 0 / 0 | `1d15ef2d82971ade…` |
| 2 | **PASS** | **40.97 MHz** | **+6.843 ns** | **0.000 / 0.000, every domain** | 12,256 / 41,910 (29 %) | 0 / 0 / 0 | `eb8ef16c420977fb…` |

| # | bar | verdict |
|---|---|---|
| E-1 | `gen_ucore_qsf --check` before each draw | **MET** |
| E-2 | 0 errors, all stages Successful, 0 latches, 0 `lpm_divide` | **MET** |
| E-3 | Fmax ≥ 32 MHz | **MET** (40.97) |
| E-4 | worst setup > 0 | **MET** (+6.843 ns) |
| E-5 | TNS 0.000 setup **and** hold, every domain | **MET** |
| E-6 | the receipt **self-labels RETENTION** | **MET** — `RETENTION (X1_AD_RETENTION=1) -- DERIVED from …flow.rpt, …map.rpt` on both, `configuration_detail.retention true` |
| E-7 | the two draws agree | **MET, in its strongest form** — identical on all four bars, identical 88-file input manifest `16293f271829d5e5…`, and the **`.rbf` is BYTE-IDENTICAL**, `7d4e887639d021ba…` on both |
| E-8 | `.qsf` regenerated and re-checked after each draw | **MET** |

The `.sof` sha256 differs between draws (`a34469bfcfa6148a…` / `060215e43c5de9b3…`)
because Quartus stamps it; the `.rbf` is the bitstream identity and it matches,
which is the check `9fbbc55a91` used for this branch's earlier retention pair.
All four receipt-declared outputs were re-hashed on disk and matched byte for
byte **before** the flash.

**⚠ READ THE BAND, NOT THE NUMBER.**  This branch's CONTROL draws are
39.16 · 39.37 · 39.47 · 39.63 · 39.81 · 40.11 MHz.  **40.97 on the RETENTION
configuration sits above all six**, which is unusual — retention has
historically drawn at or below control — and it is **REPORTED, NOT EXPLAINED**.
`standing_gates.md` §A governs unchanged: the same tree has drawn 19.42 and
45.91 MHz, the multi-seed worst-of-N gate is still not built, and **one green
build is not closure and two are not either.**  The pre-registration
deliberately registered **no Fmax point estimate**; E-3/E-4/E-5 are the bars.

---

## 2. THE FLASH AND FIRST LIGHT (`F-1` … `F-8`)

**FLASH #14 is on the board.**  `nec_test_ucore.sof`
**`060215e43c5de9b397dec0d5d8d5f4577705ee4ebd60d7d7229b15fbb3bca33b`**,
`.rbf` **`7d4e887639d021ba…`**, built from `2fa3dd33b3` **WITH
`X1_AD_RETENTION=1`** (draw 2), through `sw/safe_flash.sh` with its VERIFY leg.
**IT IS THE FIRST BITSTREAM TO CARRY F58, A1, C1, C2 AND D1** — five landings at
once, none of which had ever been in fabric.

| # | bar | registered | measured | verdict |
|---|---|---|---|---|
| F-1 | flash + ledger | VERIFY OK, `flash_log.jsonl` 16 → 17 | **VERIFY ok on try 1** (`pwr_good True`, `cpu_running True`, `use_core False`), **17 entries** | **MET** |
| F-2 | first light | MATCH 800 on all three legs | **chip-vs-golden MATCH 800 · core-vs-chip MATCH 800 · core-vs-golden MATCH 800**, `rc 0` | **MET** |
| F-3 | RBCHECK | exactly 8 registers | **8 registers round-tripped** (`EVT_ADDR[0..2]`, `EVT_CFG[0..2]`, `TVEC`, `VECCTL`) | **MET** |
| F-4 | C-6 control | 9 legs / 51 checks / 51 PASS | **51 / 51**, `failing_legs []` | **MET** |
| F-5 | `use_core=0` chip proof after everything | MATCH 800 | see §7 | see §7 |
| F-6 | `div_guard` tally | 0 UNPINNED | see §7 | see §7 |
| F-7 | transport | 0 `RigMismatch` | 0 | **MET** |
| F-8 | `board_idle()` | clean, `use_core=0` left | see §7 | see §7 |

**F-4 in detail**, scored on its verdicts as registered (§3 of the prereg — row
sha256 is not a scored quantity here):

    R1  RBCHECK                                  1/1
    P1  pin_int  hold 2      run [[547, 2]]      5/5
    P2  pin_int  hold 300    run [[547, 300]]    5/5
    P3  pin_nmi  hold 2      run [[547, 2]]      5/5
    P4  pin_nmi  hold 300    run [[547, 300]]    5/5   <- the cell the corpus CANNOT produce
    P5  pin_nmi  hold 20     run [[547, 20]]     5/5
    V1  TVEC 0000:bf00 -> 0bf00                  9/9
    V2  TVEC 0bf0:0008 -> 0bf08                  9/9
    N1  vecsub_en = 0 negative control           7/7
                                        total   51/51

`holds_proved [2, 20, 300]` · `pins_proved [pin_int, pin_nmi]` ·
`tvecs_proved [[0, 48896], [3056, 8]]` · INTA vector **measured 0xFF** ·
P1–P5 run lengths **2 · 300 · 2 · 300 · 20 to the clock** · 0 unpinned div
readbacks over the leg's 10 guards.

**P4 is the INV-1 axis and it is clean**: 300 clocks on NMI, the directive the
corpus itself can never produce (§20.3), applied to the clock.

---

## 3. THE RE-CAPTURE (`G-1` … `G-6`)

The FLASH #13 corpus was archived **by rename** first (nothing deleted):
`fz2c` → `fz2c-F13-archive`, `fz2e` → `fz2e-F13-archive`,
`fz2_capture.json` → `fz2_capture_F13-archive.json`, plus
`fz2_bars_F13-archive.json`.

| # | bar | registered | measured | verdict |
|---|---|---|---|---|
| G-1 | corpus identity | `SEED_LIST_SHA256 45d25f31a325c496…`, 3,840 | **identical**, 960 + 2,880 | **MET** |
| G-2 | preflight | `verdict OK`, `board_leg true`, 0 GEN_DRIFT | **OK**, board leg run, **192 seeds hits=0** | **MET** |
| G-3 | completeness | 48/48 strata, `rc 0`, no `halted` | **48/48**, `halted: null`, **3,840 seeds in 11.1 min** (667.9 s board) | **MET** |
| G-4 | retained captures | **725** | **677** (`fz2c` 499 + `fz2e` 178) | **MISSED — and the bar was mis-registered, see below** |
| G-5 | flash pin | every row's era = FLASH #14 | **`distinct_eras 1`, `absent 0`, `build_stale 0`** over 3,840 (bars C-4) | **MET** |
| G-6 | discards | **3**, the same three, denominator 3,837 | **1** — `fz2e/509069` only; denominator **3,839** | **MISSED — itemized in §5.3** |

**G-4 IS A MISS AND THE BAR WAS MINE AND WRONG.**  I registered 725 as if it
were a constant.  It is not: the enriched population's retention is
**divergence-driven** (`keep_rows_every = 0`), so **a seed that stops diverging
stops being retained**.  677 is the arithmetic consequence of 56 seeds closing —
it moved in the direction the sitting exists to produce.  `bars` records the
same quantity independently (`C-3/R1_detected/retained_scored` **725 → 677**).
**Every seed named in any prediction below is still retained and was scored.**

---

## 4. `C-CHIP` — **MISSED AS REGISTERED, AND THE CORRECTED READING IS 673 / 674**

Addendum A-1 registered: all 3,840 chip-side scalar fields identical, and all
retained `real` row streams **byte-identical**.

    C-CHIP.a  arch_words / done_real / arch_ok    3,839 / 3,840   fz2c/408029
              ps3_8080                            3,838 / 3,840   fz2c/408029, fz2e/523042
              wrote_term / wrote_term_at / arch_restart  3,840 / 3,840
    C-CHIP.b  RAW, every row                        272 / 674
              SCORED rows only (from row 9)         673 / 674     fz2e/527051

**AS REGISTERED, C-CHIP FAILS.**  It is reported as a MISS.

**AND MY REGISTERED FORM OF (b) WAS WRONG IN THE EXACT WAY I SAID IT WOULD NOT
BE.**  Addendum A-1 asserted that the C-6 row-sha caveat "does not extend to
C-CHIP".  Measured: of the **402** retained seeds whose raw `real` stream
differs, **401 differ ONLY at row indices ≤ 8** — the capture's leading nine
rows.  That is not a chip movement; it is the column policy's own pipeline
prefix.  `check_boot`'s mission-G policy, which every comparator in this tree
uses, takes `qs` from release, `bs` from release+8 and **`addr`/`data`/`ps` from
release+9**, so rows 0-8 are outside what anything scores.  Every one of the 401
differs only in `ad_addr` / `ad_data` / `ps` on those rows.  **Corrected against
the artifact rather than restated: on the rows the comparator scores, the chip
leg reproduces on 673 of 674.**

**THE ONE GENUINE CHIP MOVER, AND THE TWO SCALAR MOVERS, ARE ALL THE SAME
CLASS.**

| seed | what moved | F13 `escaped_n` |
|---|---|---|
| `fz2e/527051` | 921 scored rows from row 156; family `D3`, stays a failure both eras (`bad` 1011 → 976) | escapes |
| `fz2c/408029` | `ps3_8080` T→F, `mech` REACHED→**STALLED**, `done_real` T→F | **45** |
| `fz2e/523042` | `ps3_8080` T→F, `sub` open_bus→clean | **14** |

**All three are ESCAPED seeds — programs that leave the image and execute open
bus.**  That is the simplest available statement and it needs no new mechanism:
a program with no defined instruction stream has no reproducible bus, and the
two seeds whose runtime 8080 entry made them A-12 discards were 8080-entering
*because* they escaped.  The escaped population is 1,113 of 3,840.

**DISPOSITION, PER THE REGISTERED RULE.**  A-1 registered that a C-CHIP failure
is "a hard stop for **attribution**, not for the sitting", and that a partial
failure must be itemized seed by seed.  It is partial and it is itemized: **the
three affected seeds are `fz2e/527051`, `fz2c/408029` and `fz2e/523042`, none of
which appears in any seat prediction**, and **no per-landing claim in §5 rests
on any of them**.  Attribution stands for the other 3,837.  The archived chip
rows were **not** substituted for anything.

---

## 5. THE SEAT-LEVEL SCORING

`python3 sw/fz2_f14_score.py --new sw/testdata/fz2/fz2_failure_ledger_f14_2026-08-10.json`,
whose seat lists are transcribed from the pre-registration and not re-derived.

### 5.1 **EVERY REGISTERED SEAT CLOSED — 46 OF 46**

| # | landing | registered | **measured** | verdict |
|---|---|---|---|---|
| P-1 | `A1` `483c1f1270` | 6 | **6 / 6** | **MET** |
| P-2 | `F58` `b245334d7c` | 23 | **23 / 23** | **MET** |
| P-3 | `C1` `97ca50861c` | 12 | **12 / 12** | **MET** |
| P-4 | `C2` `93af2a75cd` | 2 | **2 / 2** | **MET** |
| P-5 | `D1` `c64f99e6a9` | 3 | **3 / 3** | **MET** |

**All five landings performed in fabric exactly as their offline instruments
said they would, seat for seat, with no seat unaccounted for.**  P-3's 12 and
P-4's 2 — the fourteen the erratum's `P-8` was written about — **all left the
ledger**, which settles the question E-1 opened: the architectural dump was
never a second gate, and it did not become one.

### 5.2 THE SEATS REGISTERED TO STAY

| clause | registered | measured | verdict |
|---|---|---|---|
| **P-1b** the 12 `A1` downstream seats stay, `first_bad` LATER | 12 stay | **12 stay, all `+1` exactly**, 0 earlier | **MET** |
| **P-1b** `fz2e/518009` must not move at all | unmoved | **first 3326 → 3326, rows 279 → 279** | **MET** |
| **P-2** `fz2c/407064` must not close | stays | **stays** | **MET** |
| **P-3b** `C1`'s 9 non-carriers | stay | **9 stay** | **MET** |
| **P-3c** the §64.1 counter-population | not one row | **4 / 4 identical in `first_bad` AND `diverging_rows`** | **MET** |
| **P-4b** `fz2e/513019` improves, does not close | stays | **stays** | **MET** |
| **P-4c** the four `C2a` seats | stay | **4 stay** | **MET** |
| **P-4d** **`fz2c/404040` MUST NOT APPEAR** | absent | **ABSENT** | **MET** |
| **P-6** 92 seeds in nine untouched families | 0 exits | **0 exits, all nine families** | **MET** |
| **P-1b / P-5b** two registered non-closers DID close | stay | `fz2e/521075`, `fz2e/528069` | **MISSED — an unregistered GAIN, §5.4** |

**P-1b's `+1` prediction landed on twelve seeds out of twelve, to the row**
(3045→3046, 3252→3253, 3321→3322, 3038→3039, 3317→3318, 3317→3318, 3329→3330,
3263→3264, 3032→3033, 3019→3020, 3165→3166, 3272→3273).  That is the A1
sitting's "display-only, and a second downstream mechanism remains" reading
confirmed in silicon, one clock at a time.

**P-4d is the falsifier and it did not fire.**  `fz2c/404040` — the seed whose
silicon behaviour refuted `C2`'s registered form — is a passing seed on FLASH
#14.  The landed form is the one that preserves it.

### 5.3 THE DENOMINATOR MOVED, AND IT IS REPORTED LOUDLY

**Two of the three A-12 discards stopped having a runtime 8080 entry.**
`fz2c/408029` and `fz2e/523042` read `ps3_8080 = False` on FLASH #14, so by the
A-12 rule as written they are **not** discarded and they enter the denominator;
both are `bad_rows == 0` and both count as matched.  `fz2e/509069` remains the
only discard.

`_ps3_8080()` is a **SOCKET-leg** predicate by amendment A-2, so this is a chip
movement, not a core one — and it is §4's finding: both seeds escape the image.
G-6 registered this exact possibility and required the rate to be re-derived on
the new denominator with the change itemized.  **Both bases are therefore given
in §6, and the ratchet is quoted on the like-for-like one.**

### 5.4 **TEN UNREGISTERED CLOSURES — ITEMIZED, NOT FOLDED INTO THE HEADLINE**

56 seeds left the ledger; 46 were registered.  The other ten:

| seed | ledger family | was |
|---|---|---|
| `fz2c/407019` | C1 | first 3108, 580 rows |
| `fz2e/512021` | C1 | first 3177, 663 rows |
| `fz2e/512027` | C1 | first 3170, 644 rows |
| `fz2e/528035` | C1 | first 3363, 491 rows |
| `fz2e/529040` | C1 | first 3247, 580 rows |
| `fz2e/530024` | C1 | first 3138, 665 rows |
| `fz2e/531036` | C1 | first 1272, 2139 rows |
| `fz2e/515042` | A1 | first 539, 4 rows |
| `fz2e/521075` | A1 | first 3089, 2 rows |
| `fz2e/528069` | A2 | first 3264, 1 row |

**Seven of the ten are `C1`-family**, so the `C1` landing closed **19 of the 29**
in fabric where its offline instrument could only show 12.  That instrument
(`fz2_c1_rescore`) spends the TB's single pin-event scheduler on the terminating
NMI and cuts its own window; the fabric leg has the seed's real stimulus and the
corpus's own window.  **This is the `tb_v30_core`-vs-fabric gap the erratum
named as the real residual risk, and it ran in the landings' favour rather than
against them.**  `fz2e/521075` and `fz2e/528069` were registered non-closers on
the same instruments and are the same effect.

**No unregistered closure is claimed as a new mechanism.**  Each is a member of
a family a landing already owns, and each is reported here rather than absorbed.

### 5.5 **P-7 MISSED — THREE SEEDS ENTERED THE LEDGER**

| seed | F13 | F14 | `first_bad` | rows | what changed |
|---|---|---|---|---|---|
| `fz2e/517043` | SUCCESS | **FUNCTIONAL** `done_mismatch` | 2773 | 619 | **`done_sim` True → False** — the CORE no longer reaches the terminator |
| `fz2e/531009` | SUCCESS | KNOWN_ACCEPTED `open_bus` | 3325 | 577 | `arch_match` True → False |
| `fz2e/532032` | SUCCESS | KNOWN_ACCEPTED `open_bus` | 3029 | **2** | rows only, `arch` still OK |

All three have an **identical chip leg** (none is a C-CHIP mover), so **all three
are core-side and belong to this landing set.**  They are reported as a MISS
against a registered ZERO.

**AND THEY PARTITION CLEANLY, WHICH IS THE USEFUL PART.**

* **`fz2e/517043` IS A `PSW.TF` SEED, AND IT IS `C1`'s.**  The corpus carries
  **101** seeds with `PSW.TF` set.  Of those, **12 failed on FLASH #13 and 7 fail
  on FLASH #14**: six closed (`fz2c/400007`, `fz2c/400019`, `fz2c/400022`,
  `fz2c/402075`, `fz2e/512021`, `fz2e/512027`) and **exactly one newly failed**.
  On the population the `C1` landing actually exercises, it is **net +5, and its
  one loss is inside its own mechanism.**  `C1`'s results §4.1 registered a debt
  in advance — *"THIS LANDING WAS NOT CHECKED AGAINST THE TRAP'S OWN DIRECTED
  CELL"*, `sm3_tf_floor_cell.py` being unrunnable on this branch since the
  fuzz-v2 image anchor moved.  **`fz2e/517043` is the first measured cost of that
  debt**, it is a directed-cell question exactly as §3 of that document said the
  residue would be, and it is **not** repaired here by inspection.
* **`fz2e/531009` and `fz2e/532032` are ESCAPED raw seeds** — `escaped_n` **26**
  and **127**, `n_ins = 0` (pure random bytes), both `sub = open_bus`.  They sit
  in the same 1,113-seed population as §4's chip movers, where the failing count
  went **110 → 84**.  Two moved the wrong way inside a population whose members
  execute open bus by construction.

**No fitted correction is proposed for any of the three and none is folded into
the headline.**  They are three named populations for a later sitting to
pre-register against.

### 5.6 P-8 (non-gating, added by erratum E-1)

Registered: on `C1`'s 12 and `C2`'s 2, `arch_match` becomes `True`
(baseline 0 / 14).  **Measured: all 14 left the ledger entirely** — rows and
dump together.  The mechanism claim holds; the clause is subsumed by §5.1.
`fz2c/407019`, an unregistered `C1` closure, is the clearest single reading:
`arch_match` **False → True** with its rows going clean in the same step.

---

## 6. THE HEADLINE

**Both bases are given, because the denominator moved (§5.3).**

### 6.1 LIKE FOR LIKE, on the REGISTERED denominator of 3,837 — this is the ratchet

    SEED MATCH   3,639 -> 3,692 / 3,837   =  94.8397 %  ->  96.2210 %
    ROW  MATCH   11,159,527 -> 11,171,232 / 11,322,230
                            =  98.5630 %  ->  98.6664 %

**Against the registered PRIMARY of 3,685 / 3,837 = 96.0386 %: +7 seeds.**
Against the registered FLOOR of 3,671: **+21**.  **Both figures rose**, which is
what §38.4's dual-reporting rule exists to check.

### 6.2 AS DERIVED, on the FLASH #14 denominator of 3,839

    SEED MATCH   3,694 / 3,839  =  96.2230 %       failures 198 -> 145
    ROW  MATCH   11,179,232 / 11,330,230  =  98.6673 %

### 6.3 The family table

| ledger family | F13 | predicted | **F14** | |
|---|---|---|---|---|
| A1 qs-pop one clock late | 23 | 17 | **15** | 2 better |
| A2 qs-pop other offset | 8 | 5 | **4** | 1 better |
| A3 cycle-time slip (non-qs) | 15 | 15 | **15** | MET |
| B1 HALT-cycle address | 24 | 1 | **1** | **MET** |
| B2 HALT entry (one leg only) | 10 | 10 | **10** | MET |
| C1 vector-1 trap MISSED by core | 29 | 17 | **10** | **7 better** |
| C2 INTA-vectored delivery | 12 | 10 | **10** | **MET** |
| C3 NMI(vec2) entry | 1 | 1 | **1** | MET |
| C4 other-vector delivery | 1 | 1 | **1** | MET |
| D1 chip fetched, core did not | 10 | 10 | **10** | MET |
| D2 core fetched, chip did not | 10 | 10 | **10** | MET |
| D3 both fetched, different address | 10 | 10 | **10** | MET |
| E1 same-status data cycle, different address | 41 | 41 | **41** | MET |
| E2 different-status data cycle | 4 | 4 | **4** | MET |
| **NEW/UNCLASSIFIED** | — | **0** | **3** | **P-7 MISSED** |
| **total** | **198** | 152 | **145** | |

Three registered family cells missed **by closing more than predicted**; one
missed by gaining three seeds.  Both directions are reported as registered.

### 6.4 The §38.9 missed-trap overlay

Registered **40 → 28**; measured **40 → 21**.  **MISSED, by 7 more closures** —
the same seven unregistered `C1` seats of §5.4.  It remains the largest single
mechanism in the corpus at 21 of 145 (14.5 %), down from 40 of 198 (20.2 %).

### 6.5 The decompositions, never one aggregate

    census    rows-exact  907 -> 924 / 960    arch-exact  849 -> 855
    enriched  rows-exact 2732 -> 2770 / 2880  arch-exact 2579 -> 2592
    tier soup rows-exact 1879 -> 1888 / 1920
    tier raw  rows-exact 1760 -> 1806 / 1920

The **census** figures are population rates (its bank rule is frozen
arithmetic); the enriched figures are not and are never pooled with them.

---

## 7. `bars`, LEAF-DIFFED

`python3 sw/fz2_w1.py bars --jobs 4` → **FZ2 BARS: 11 / 11 MET**, unchanged from
FLASH #13, and `python3 sw/fz2_leafdiff.py` against `fz2_bars_F13-archive.json`
confirms leaf by leaf that **no bar verdict moved**: 1,050 leaves compared,
**36 A-only, 0 B-only, 135 MOVED**, and every moved leaf is either a closure
gain, the two flipped discards, or the retention count.  The load-bearing ones:

    C-3 ps3_8080                    3 -> 1     (§5.3)
    C-3 R1_detected/retained_scored 725 -> 677 (G-4, §3)
    C-8 div_guards 63, unpinned 0
    C-1 excluded_ps3_8080_total     3 -> 1
    pops/census arch_exact_mean_pct 88.44 -> 89.06

The 36 A-only leaves are the two discard records that no longer exist and stale
`tree_dirty` rows from the F13 run.

---

## 8. BOARD HYGIENE

| | |
|---|---|
| `div_guard` total | **63** — preflight 2 (pre-flash) + control 10 + preflight 2 (post-flash) + capture 48 + idle 1 |
| **UNPINNED** | **0** — F-6 **MET** |
| `RigMismatch` | **0** — F-7 **MET** |
| quarantines / run-error lines | **0 / 0** (bars C-10) |
| `check_ab_hw chip 800` after everything | **MATCH over 800 rows** — F-5 **MET** |
| `board_idle()` | **OK, `use_core = 0` left selected** — F-8 **MET** |

`board_idle` was run **last**, after the chip proof, so the board is left on a
one-byte NOP with the socket selected.  The registered order named it first;
the deviation is stated because it changes what the board is left doing, and it
is the safer end state.  `fz2_idle.json` records `board_procs_after` containing
the transport's own `v30ctl.py serve`, which is the connection this session
opened and is the same row every prior sitting has recorded.

---

## 9. WHAT THIS SITTING ESTABLISHES, AND WHAT IT DOES NOT

**ESTABLISHES.**  **All five landings are in silicon and all five work.**  Every
one of the 46 registered seats closed, seat for seat, on a bitstream that had
never carried any of them; 10 more closed than were registered; the 92 seeds in
the nine families no landing touches did not move; the four-seed §64.1
counter-population did not move by a row; and `C2`'s own falsifier, the seed
whose silicon behaviour refuted its first form, is still a pass.  The corpus
headline goes **3,639 → 3,692 of 3,837** on the registered denominator, and the
row rate rises with it.  `F58`'s 23 `B1` seats and `C1`'s 19 `C1` seats are the
bulk of it.

**DOES NOT ESTABLISH.**

* **That nothing was lost.**  Three seeds entered the ledger and P-7 is a MISS.
  `fz2e/517043` is a `PSW.TF` seed and belongs to `C1`, whose directed cell
  **still cannot run on this branch**; that debt is now measured, not
  hypothetical, and it is not discharged.
* **That the chip leg is a perfect reference.**  C-CHIP missed as registered.
  On the scored rows it is 673/674, and every mover is an escaped seed — but
  "escaped seeds do not reproduce" is a **reading of three seeds**, not a law,
  and it has no falsifier written for it yet.
* **Any Fmax closure.**  Two identical green draws are not a distribution.
* Nothing about `wr1`, `b2`, the four `_v1_anchor_stop` timed gates or
  `sm3_tf_floor_cell` — all still `GEN_DRIFT` or anchor-broken on this branch.
* **No mechanism is claimed for the 145 survivors.**  `E1` (41) and the `D*`
  families (30) are untouched and unexplained, exactly as they were.

